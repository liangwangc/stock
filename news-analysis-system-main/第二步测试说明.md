# 第二步：测试股票匹配功能

**步骤**：测试股票匹配模块是否正常工作

---

## 执行步骤

### 1. 安装依赖（如果需要）

```bash
cd d:\wjw_work\news-analysis-system-main
py -m pip install sqlalchemy pandas pymysql
```

---

### 2. 运行测试脚本

```bash
cd d:\wjw_work\news-analysis-system-main
py test_stock_mapper.py
```

---

### 3. 预期结果

**成功输出示例**：
```
================================================================================
测试股票匹配模块
================================================================================

1. 初始化股票匹配器...
   正在加载股票信息...
   已加载 5400 只股票信息到缓存
   [成功] 股票匹配器初始化完成

2. 测试案例1：包含股票代码的新闻
   标题: 贵州茅台发布2024年业绩报告
   内容: 贵州茅台(600519)今日发布2024年业绩报告，净利润同比增长15%
   匹配结果:
   - 主要股票: 600519
   - 所有股票: ['600519']
   - 板块: 消费
   - 行业: 白酒
   - 相关性得分: 1.0

3. 测试案例2：包含股票名称的新闻
   ...

================================================================================
测试总结
================================================================================
[成功] 股票匹配模块测试完成
================================================================================
```

---

### 4. 如果测试失败

**检查项**：
1. ✅ 依赖包是否安装（sqlalchemy, pandas, pymysql）
2. ✅ 数据库配置是否正确（`src/config/config.py`）
3. ✅ 主数据库是否可访问（stock_data）
4. ✅ stock_predictions 表是否有数据

**验证数据库连接**：
```python
from src.config.config import MAIN_DATABASE_URL
from sqlalchemy import create_engine

engine = create_engine(MAIN_DATABASE_URL)
with engine.connect() as conn:
    result = conn.execute("SELECT COUNT(*) FROM stock_predictions")
    print(f"股票数量: {result.fetchone()[0]}")
```

---

## 下一步

测试成功后，继续第三步：测试新闻获取和股票匹配

```bash
py test_get_news_once.py
```
