# 网络连接错误重试机制优化说明

**创建日期**：2026-01-22  
**问题描述**：长时间运行的数据采集任务出现网络连接中断错误

---

## 一、问题分析

### 1.1 错误现象

```
2026-01-22 17:01:50 - data_source.stock_data_source - WARNING - 调用 stock_zh_a_spot_em 时遇到代理连接问题: ('Connection aborted.', RemoteDisconnected('Remote end closed connection without response'))
2026-01-22 17:01:50 - utils.stock_history_collector - WARNING - 无法找到 300552 2026-01-22 的精确数据，跳过该股票该日期的数据获取
```

### 1.2 问题原因

1. **网络连接不稳定**：长时间运行后，网络连接可能出现中断
2. **API限流**：频繁调用API可能导致限流
3. **连接池耗尽**：长时间运行可能导致连接池资源耗尽
4. **代理服务器超时**：代理服务器可能因为超时而关闭连接
5. **缺少重试机制**：原代码遇到网络错误直接抛出异常，没有重试机制

---

## 二、解决方案

### 2.1 添加重试机制

在 `_call_akshare_without_proxy` 函数中添加了重试机制：

**代码位置**：`data_source/stock_data_source.py:19-150`

**重试策略**：
- **最大重试次数**：3次
- **初始延迟**：2秒
- **退避策略**：指数退避（2秒、4秒、8秒）
- **重试条件**：仅对网络连接错误进行重试

**识别的网络错误类型**：
- `Connection aborted`
- `RemoteDisconnected`
- `Connection` 相关错误
- `Timeout` 相关错误
- `ECONNRESET`
- `Broken pipe`
- `Connection reset`

---

### 2.2 代码实现

```python
def _call_akshare_without_proxy(func: Callable, *args, **kwargs) -> Any:
    """
    在禁用代理的环境下调用akshare函数（带重试机制）
    """
    max_retries = 3  # 最大重试次数
    retry_delay = 2  # 初始重试延迟（秒）
    
    for attempt in range(max_retries):
        try:
            # ... 代理设置代码 ...
            
            # 调用函数
            result = func(*args, **kwargs)
            return result
            
        except Exception as e:
            error_str = str(e)
            error_type = type(e).__name__
            
            # 检查是否是网络连接错误（需要重试）
            is_network_error = (
                'Connection aborted' in error_str or
                'RemoteDisconnected' in error_str or
                'Connection' in error_type or
                'Timeout' in error_type or
                'timeout' in error_str.lower() or
                'ECONNRESET' in error_str or
                'Broken pipe' in error_str or
                'Connection reset' in error_str.lower()
            )
            
            # 如果是网络错误且还有重试机会，则重试
            if is_network_error and attempt < max_retries - 1:
                wait_time = retry_delay * (2 ** attempt)  # 指数退避
                logger.warning(f"调用 {func.__name__} 时遇到网络连接问题（第 {attempt + 1}/{max_retries} 次尝试），等待 {wait_time:.1f} 秒后重试: {error_str}")
                time.sleep(wait_time)
                continue
            else:
                # 非网络错误或重试次数用完，记录错误并抛出异常
                if attempt == max_retries - 1:
                    logger.error(f"调用 {func.__name__} 失败（已重试 {max_retries} 次）: {error_str}")
                else:
                    logger.warning(f"调用 {func.__name__} 时遇到错误: {error_str}")
                raise
```

---

## 三、优化效果

### 3.1 错误处理改进

**优化前**：
- ❌ 遇到网络错误直接抛出异常
- ❌ 任务中断，需要手动重启
- ❌ 无法自动恢复

**优化后**：
- ✅ 自动识别网络错误
- ✅ 自动重试（最多3次）
- ✅ 指数退避策略，避免频繁重试
- ✅ 非网络错误立即抛出，不浪费时间

---

### 3.2 日志输出示例

**重试时的日志**：
```
2026-01-22 17:01:50 - data_source.stock_data_source - WARNING - 调用 stock_zh_a_spot_em 时遇到网络连接问题（第 1/3 次尝试），等待 2.0 秒后重试: ('Connection aborted.', RemoteDisconnected('Remote end closed connection without response'))
2026-01-22 17:01:52 - data_source.stock_data_source - INFO - 调用 stock_zh_a_spot_em 成功
```

**重试失败时的日志**：
```
2026-01-22 17:01:50 - data_source.stock_data_source - WARNING - 调用 stock_zh_a_spot_em 时遇到网络连接问题（第 1/3 次尝试），等待 2.0 秒后重试: ...
2026-01-22 17:01:52 - data_source.stock_data_source - WARNING - 调用 stock_zh_a_spot_em 时遇到网络连接问题（第 2/3 次尝试），等待 4.0 秒后重试: ...
2026-01-22 17:01:56 - data_source.stock_data_source - WARNING - 调用 stock_zh_a_spot_em 时遇到网络连接问题（第 3/3 次尝试），等待 8.0 秒后重试: ...
2026-01-22 17:02:04 - data_source.stock_data_source - ERROR - 调用 stock_zh_a_spot_em 失败（已重试 3 次）: ...
```

---

## 四、适用场景

### 4.1 适用的情况

- ✅ 网络连接不稳定
- ✅ API限流导致的临时错误
- ✅ 代理服务器超时
- ✅ 连接池资源暂时耗尽

### 4.2 不适用的情况

- ❌ API返回的业务错误（如股票代码不存在）
- ❌ 认证失败
- ❌ 参数错误

---

## 五、注意事项

### 5.1 重试延迟

- **初始延迟**：2秒
- **指数退避**：每次重试延迟时间翻倍
- **总等待时间**：最多 2 + 4 + 8 = 14秒（如果3次都失败）

### 5.2 性能影响

- **正常情况**：无影响（不重试）
- **网络错误**：最多增加14秒延迟（3次重试）
- **建议**：如果网络错误频繁，考虑增加重试次数或调整延迟时间

### 5.3 长时间运行任务

- ✅ 自动处理临时网络错误
- ✅ 减少任务中断次数
- ✅ 提高数据采集成功率

---

## 六、后续优化建议

### 6.1 可配置的重试参数

可以考虑将重试参数配置化：

```python
# config.py
API_RETRY_CONFIG = {
    'max_retries': 3,
    'initial_delay': 2,
    'backoff_multiplier': 2,
    'max_delay': 30
}
```

### 6.2 连接池管理

- 定期清理无效连接
- 监控连接池使用情况
- 自动重建连接池

### 6.3 监控和告警

- 记录重试次数和成功率
- 网络错误频率告警
- 任务执行时间监控

---

**文档完成时间**：2026-01-22  
**状态**：✅ 优化完成
