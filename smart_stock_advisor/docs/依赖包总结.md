# 项目依赖包总结

本文档总结了项目中所有需要安装的 Python 包，方便在其他环境部署时参考。

## 📦 快速参考

### 一键安装所有依赖

```bash
pip install -r requirements.txt
```

### 使用国内镜像（推荐）

```bash
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
```

---

## 📋 完整依赖列表

### 核心依赖（必需，共 12 个）

| 包名 | 版本要求 | 用途 | 安装命令 |
|------|---------|------|---------|
| pandas | >=2.0.0 | 数据处理和分析 | `pip install pandas>=2.0.0` |
| numpy | >=1.24.0 | 数值计算 | `pip install numpy>=1.24.0` |
| akshare | >=1.11.0 | 中国股票数据获取 | `pip install akshare>=1.11.0` |
| requests | >=2.28.0 | HTTP 请求库 | `pip install requests>=2.28.0` |
| beautifulsoup4 | >=4.11.0 | HTML 解析（新闻抓取） | `pip install beautifulsoup4>=4.11.0` |
| lxml | >=4.9.0 | XML/HTML 解析器 | `pip install lxml>=4.9.0` |
| python-dateutil | >=2.8.0 | 日期时间处理 | `pip install python-dateutil>=2.8.0` |
| pymysql | >=1.1.0 | MySQL 数据库驱动 | `pip install pymysql>=1.1.0` |
| flask | >=2.3.0 | Web 框架 | `pip install flask>=2.3.0` |
| flask-socketio | - | WebSocket 支持 | `pip install flask-socketio` |
| schedule | >=5.3.0 | 定时任务调度 | `pip install schedule>=5.3.0` |
| tqdm | >=4.65.0 | 进度条显示 | `pip install tqdm>=4.65.0` |
| werkzeug | >=2.3.0 | Flask 依赖 | `pip install werkzeug>=2.3.0` |

### 可选依赖（增强功能，共 10 个）

| 包名 | 版本要求 | 用途 | 是否必需 |
|------|---------|------|---------|
| scikit-learn | >=1.3.0 | 机器学习库（模型优化） | 否 |
| matplotlib | >=3.7.0 | 基础图表库 | 否 |
| seaborn | >=0.12.0 | 统计图表库 | 否 |
| plotly | >=5.17.0 | 交互式图表 | 否 |
| dash | >=2.14.0 | Web 可视化 | 否 |
| yfinance | >=0.2.0 | 美股数据获取 | 仅美股功能需要 |
| jieba | >=0.42.1 | 中文分词（情感分析） | 否（推荐） |
| psutil | >=5.9.0 | 系统监控 | 否 |
| pytest | >=7.4.0 | 单元测试框架 | 仅开发环境 |
| pytest-cov | >=4.1.0 | 测试覆盖率 | 仅开发环境 |
| pytest-mock | >=3.11.0 | Mock 支持 | 仅开发环境 |
| openpyxl | >=3.1.0 | Excel 文件读写 | quant_trading_platform 需要 |

---

## 🎯 按功能模块分类

### 1. 数据获取模块

```bash
pip install akshare>=1.11.0      # 中国股票数据
pip install yfinance>=0.2.0      # 美股数据（可选）
pip install requests>=2.28.0    # HTTP 请求
pip install beautifulsoup4>=4.11.0  # HTML 解析
pip install lxml>=4.9.0         # XML/HTML 解析器
```

### 2. 数据处理模块

```bash
pip install pandas>=2.0.0
pip install numpy>=1.24.0
pip install python-dateutil>=2.8.0
```

### 3. Web 应用模块

```bash
pip install flask>=2.3.0
pip install flask-socketio
pip install werkzeug>=2.3.0
```

### 4. 数据库模块

```bash
pip install pymysql>=1.1.0
```

### 5. 任务调度模块

```bash
pip install schedule>=5.3.0
pip install tqdm>=4.65.0
```

### 6. 机器学习模块（可选）

```bash
pip install scikit-learn>=1.3.0
```

### 7. 可视化模块（可选）

```bash
pip install matplotlib>=3.7.0
pip install seaborn>=0.12.0
pip install plotly>=5.17.0
pip install dash>=2.14.0
```

### 8. 新闻分析模块（可选）

```bash
pip install jieba>=0.42.1       # 中文分词（推荐）
```

### 9. 系统监控模块（可选）

```bash
pip install psutil>=5.9.0
```

### 10. 测试模块（开发环境）

```bash
pip install pytest>=7.4.0
pip install pytest-cov>=4.1.0
pip install pytest-mock>=3.11.0
```

---

## 📝 安装命令汇总

### 最小化安装（核心功能）

```bash
pip install pandas>=2.0.0 numpy>=1.24.0 akshare>=1.11.0 \
    requests>=2.28.0 beautifulsoup4>=4.11.0 lxml>=4.9.0 \
    python-dateutil>=2.8.0 pymysql>=1.1.0 flask>=2.3.0 \
    flask-socketio schedule>=5.3.0 tqdm>=4.65.0 werkzeug>=2.3.0
```

### 完整安装（推荐）

```bash
pip install -r requirements.txt
```

### 仅数据收集功能

```bash
pip install pandas numpy akshare requests beautifulsoup4 lxml \
    python-dateutil pymysql schedule tqdm
```

### 仅 Web 界面功能

```bash
pip install flask flask-socketio werkzeug pandas numpy pymysql
```

### 仅预测功能（无 Web）

```bash
pip install pandas numpy akshare scikit-learn matplotlib python-dateutil
```

### 新闻功能（完整）

```bash
pip install pandas numpy requests beautifulsoup4 lxml jieba \
    pymysql schedule python-dateutil
```

### 美股功能

```bash
pip install pandas numpy yfinance akshare pymysql python-dateutil
```

---

## ✅ 验证安装

运行依赖检查脚本：

```bash
python check_dependencies.py
```

或手动验证：

```python
python -c "import pandas, numpy, akshare, flask, pymysql; print('核心依赖安装成功！')"
```

---

## 🔍 常见问题

### Q: 如何知道哪些包已安装？

A: 运行 `python check_dependencies.py` 或使用 `pip list`

### Q: 安装失败怎么办？

A: 
1. 升级 pip: `pip install --upgrade pip`
2. 使用国内镜像: `pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple`
3. 单独安装失败的包: `pip install <package_name>`

### Q: 哪些包是必需的？

A: 核心依赖（12个）是必需的，可选依赖可以根据需要安装。

### Q: jieba 不安装可以吗？

A: 可以，系统会自动使用简单分词，但准确性略低。

---

## 📚 相关文档

- [依赖安装指南](./依赖安装指南.md) - 详细的安装说明和故障排除
- [安装指南](./INSTALL.md) - 快速安装步骤
- [jieba 安装指南](./jieba_installation_guide.md) - jieba 分词库说明

---

**最后更新**：2024-01-13  
**维护者**：Smart Stock Advisor Team
