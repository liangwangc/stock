# 未收盘-明天预测输入验证确认报告

**日期**: 2026-01-16  
**功能**: 主页面"未收盘-明天"预测功能  
**目标**: 确认只预测用户输入的1-10个股票，不会进行全部股票预测

---

## 一、功能确认

✅ **确认**: 主页面"未收盘-明天"预测功能**只预测用户输入的1-10个股票**，不会进行全部股票预测或其他股票预测。

---

## 二、前端验证（stock_list.html）

### 2.1 输入限制

**代码位置**: `stock_list.html:1128-1157` (`startPredict()` 函数)

**验证逻辑**:

1. **非空验证** (第1133-1136行):
```javascript
const symbolsText = symbolsInput.value.trim();
if (!symbolsText) {
    alert('请输入股票代码');
    return;
}
```

2. **数量限制** (第1146-1149行):
```javascript
if (symbols.length > 10) {
    alert('最多只能输入10个股票代码');
    return;
}
```
✅ **限制**: 最多只能输入10个股票代码

3. **格式验证** (第1151-1157行):
```javascript
// 验证股票代码格式（6位数字）
for (const symbol of symbols) {
    if (!/^\d{6}$/.test(symbol)) {
        alert(`股票代码格式错误：${symbol}（应为6位数字）`);
        return;
    }
}
```
✅ **格式要求**: 必须是6位数字

### 2.2 输入提示

**代码位置**: `stock_list.html:1090-1092`

**提示文本**:
```html
<div style="margin-top: 5px; font-size: 12px; color: #666;">
    输入股票代码，多个代码用顿号、隔开（最多10个）。例如：000001、600519、000858
</div>
```

✅ **明确提示**: 最多10个股票代码

### 2.3 输入解析

**代码位置**: `stock_list.html:1138-1139`

**解析逻辑**:
```javascript
// 解析股票代码（支持顿号、和逗号,）
const symbols = symbolsText.split(/[、,，]/).map(s => s.trim()).filter(s => s);
```

✅ **支持格式**: 支持顿号（、）、逗号（,）分隔

---

## 三、后端验证（web_app.py）

### 3.1 API端点

**代码位置**: `web_app.py:4807-4915` (`/api/predict/before_close`)

**请求方式**: `POST`

**请求体**:
```json
{
    "symbols": ["000001", "600519", "000858"]
}
```

### 3.2 后端验证逻辑

#### 3.2.1 非空验证 (第4829-4830行)

```python
if not symbols or not isinstance(symbols, list):
    return jsonify({'success': False, 'message': '股票代码列表不能为空'})
```

✅ **验证**: 必须提供股票代码列表

#### 3.2.2 数量限制 (第4832-4833行)

```python
if len(symbols) > 10:
    return jsonify({'success': False, 'message': '最多只能预测10只股票'})
```

✅ **限制**: 最多只能预测10只股票

#### 3.2.3 格式验证 (第4835-4842行)

```python
# 验证股票代码格式（防止注入和无效输入）
validated_symbols = []
for symbol in symbols:
    symbol_str = str(symbol).strip()
    # 验证：只允许6位数字
    if not symbol_str or not symbol_str.isdigit() or len(symbol_str) != 6:
        return jsonify({'success': False, 'message': f'股票代码格式错误: {symbol_str}（应为6位数字）'})
    validated_symbols.append(symbol_str.zfill(6))
```

✅ **格式要求**: 
- 必须是6位数字
- 使用 `isdigit()` 验证，防止注入
- 自动补零（`zfill(6)`）

### 3.3 预测执行逻辑

**代码位置**: `web_app.py:4854-4898`

**执行逻辑**:
```python
for symbol in validated_symbols:
    try:
        logger.info(f"开始预测股票（未收盘-明天）: {symbol}")
        
        # 执行预测（会自动使用数据库新闻和历史数据，实时获取当天数据）
        prediction_result = predictor.predict(symbol)
        
        # ... 保存结果 ...
        
    except Exception as e:
        # ... 错误处理 ...
```

✅ **确认**: 
- **只循环用户输入的股票列表** (`validated_symbols`)
- **不会预测其他股票**
- **不会进行全部股票预测**

---

## 四、验证流程总结

### 4.1 完整验证流程

```
用户输入股票代码
    ↓
前端验证（stock_list.html）
    ├─ 非空验证 ✅
    ├─ 数量限制（最多10个）✅
    └─ 格式验证（6位数字）✅
    ↓
发送POST请求到 /api/predict/before_close
    ↓
后端验证（web_app.py）
    ├─ 登录验证 ✅
    ├─ 市场状态验证（未收盘）✅
    ├─ 非空验证 ✅
    ├─ 数量限制（最多10个）✅
    └─ 格式验证（6位数字）✅
    ↓
循环预测用户输入的股票
    ├─ 只预测 validated_symbols 中的股票 ✅
    ├─ 不会预测其他股票 ✅
    └─ 不会进行全部股票预测 ✅
    ↓
返回预测结果
```

### 4.2 验证点确认

| 验证项 | 前端 | 后端 | 状态 |
|--------|------|------|------|
| 非空验证 | ✅ | ✅ | 通过 |
| 数量限制（最多10个） | ✅ | ✅ | 通过 |
| 格式验证（6位数字） | ✅ | ✅ | 通过 |
| 只预测用户输入的股票 | ✅ | ✅ | 通过 |
| 不会预测其他股票 | ✅ | ✅ | 通过 |
| 不会进行全部股票预测 | ✅ | ✅ | 通过 |

---

## 五、代码位置索引

### 5.1 前端代码

| 功能 | 文件 | 行号 | 说明 |
|------|------|------|------|
| 预测弹窗 | `stock_list.html` | 1073 | `showPredictModal()` |
| 输入验证 | `stock_list.html` | 1128 | `startPredict()` |
| 数量限制 | `stock_list.html` | 1146 | 最多10个 |
| 格式验证 | `stock_list.html` | 1151 | 6位数字 |

### 5.2 后端代码

| 功能 | 文件 | 行号 | 说明 |
|------|------|------|------|
| API端点 | `web_app.py` | 4807 | `/api/predict/before_close` |
| 数量限制 | `web_app.py` | 4832 | 最多10个 |
| 格式验证 | `web_app.py` | 4835 | 6位数字 |
| 预测执行 | `web_app.py` | 4854 | 循环预测 |

---

## 六、安全特性

### 6.1 输入验证

1. **格式验证**: 只允许6位数字，防止SQL注入和XSS攻击
2. **类型验证**: 验证输入为列表类型
3. **数量限制**: 限制最多10个股票，防止资源滥用

### 6.2 数据清理

```python
symbol_str = str(symbol).strip()  # 去除前后空格
validated_symbols.append(symbol_str.zfill(6))  # 自动补零
```

✅ **数据清理**: 去除空格，自动补零

---

## 七、测试用例

### 7.1 正常输入

**输入**: `000001、600519、000858`

**预期结果**:
- ✅ 通过验证
- ✅ 预测3只股票
- ✅ 返回预测结果

### 7.2 边界测试

**测试1: 10个股票（最大值）**
- **输入**: `000001、000002、000003、000004、000005、000006、000007、000008、000009、000010`
- **预期**: ✅ 通过验证，预测10只股票

**测试2: 11个股票（超过限制）**
- **输入**: `000001、000002、...、000011`（11个）
- **预期**: ❌ 前端提示"最多只能输入10个股票代码"，后端返回"最多只能预测10只股票"

**测试3: 格式错误**
- **输入**: `00001`（5位数字）
- **预期**: ❌ 前端提示"股票代码格式错误"，后端返回"股票代码格式错误"

**测试4: 非数字**
- **输入**: `abc123`
- **预期**: ❌ 前端提示"股票代码格式错误"，后端返回"股票代码格式错误"

### 7.3 空输入测试

**测试1: 空字符串**
- **输入**: `""`
- **预期**: ❌ 前端提示"请输入股票代码"

**测试2: 只有空格**
- **输入**: `"   "`
- **预期**: ❌ 前端提示"请输入股票代码"

---

## 八、结论

### 8.1 功能确认

✅ **完全符合要求**:
1. ✅ **只预测用户输入的股票**: 代码中只循环 `validated_symbols`，不会预测其他股票
2. ✅ **数量限制**: 前端和后端都限制最多10个股票
3. ✅ **格式验证**: 前端和后端都验证6位数字格式
4. ✅ **不会进行全部股票预测**: 没有获取全部股票列表的逻辑，只预测用户输入的股票

### 8.2 验证机制

✅ **双重验证**:
- **前端验证**: 在用户输入时立即验证，提供即时反馈
- **后端验证**: 在服务器端再次验证，确保安全性

### 8.3 安全保证

✅ **安全措施**:
- 格式验证防止注入攻击
- 数量限制防止资源滥用
- 类型验证防止恶意输入

---

## 九、相关代码片段

### 9.1 前端验证代码

```javascript
// stock_list.html:1128-1157
async function startPredict() {
    const symbolsInput = document.getElementById('predictSymbols');
    if (!symbolsInput) return;
    
    const symbolsText = symbolsInput.value.trim();
    if (!symbolsText) {
        alert('请输入股票代码');
        return;
    }
    
    // 解析股票代码（支持顿号、和逗号,）
    const symbols = symbolsText.split(/[、,，]/).map(s => s.trim()).filter(s => s);
    
    if (symbols.length === 0) {
        alert('请输入有效的股票代码');
        return;
    }
    
    if (symbols.length > 10) {
        alert('最多只能输入10个股票代码');
        return;
    }
    
    // 验证股票代码格式（6位数字）
    for (const symbol of symbols) {
        if (!/^\d{6}$/.test(symbol)) {
            alert(`股票代码格式错误：${symbol}（应为6位数字）`);
            return;
        }
    }
    
    // ... 发送请求 ...
}
```

### 9.2 后端验证代码

```python
# web_app.py:4825-4854
data = request.get_json()
symbols = data.get('symbols', [])

if not symbols or not isinstance(symbols, list):
    return jsonify({'success': False, 'message': '股票代码列表不能为空'})

if len(symbols) > 10:
    return jsonify({'success': False, 'message': '最多只能预测10只股票'})

# 验证股票代码格式（防止注入和无效输入）
validated_symbols = []
for symbol in symbols:
    symbol_str = str(symbol).strip()
    # 验证：只允许6位数字
    if not symbol_str or not symbol_str.isdigit() or len(symbol_str) != 6:
        return jsonify({'success': False, 'message': f'股票代码格式错误: {symbol_str}（应为6位数字）'})
    validated_symbols.append(symbol_str.zfill(6))

# 只预测用户输入的股票
for symbol in validated_symbols:
    prediction_result = predictor.predict(symbol)
    # ... 保存结果 ...
```

---

**报告生成时间**: 2026-01-16  
**状态**: ✅ 验证完成，功能符合要求
