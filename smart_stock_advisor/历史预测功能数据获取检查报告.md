# 历史预测功能数据获取检查报告

## 检查时间
2026-01-13

## 检查范围
- `/api/history` API端点
- `StockPredictionDB.get_predictions()` 方法
- `history.html` 前端页面
- 数据库字段映射和数据格式

## 1. API端点检查 (`/api/history`)

### 代码位置
`web_app.py` 第2466-2590行

### 数据获取流程
1. **参数验证**：
   - 从请求参数获取 `symbol`
   - 验证股票代码不为空
   - 格式化股票代码（补齐6位）

2. **数据库查询**：
   ```python
   db = StockPredictionDB()
   predictions = db.get_predictions(symbol=symbol, limit=1000, order_by='prediction_time DESC')
   ```
   - 调用 `get_predictions` 方法
   - 按预测时间倒序排列
   - 限制返回1000条记录

3. **数据更新**：
   - 在获取数据前调用 `_update_historical_actual_data()` 更新实际数据
   - 如果更新失败，不影响数据获取

4. **数据转换**：
   - 将数据库记录转换为API响应格式
   - 包含以下字段：
     - `id`, `symbol`, `name`
     - `prediction_date`, `target_date`, `prediction_time`
     - `current_price`, `predicted_close_price`, `predicted_change_pct`
     - `prediction`, `up_probability`, `down_probability`, `confidence`, `final_score`
     - `actual_price`, `actual_change_pct`, `actual_direction`, `prediction_hit`
     - `factors` (从 `prediction_factors` 表关联查询)

### 字段映射检查
✅ **正确**：所有字段都正确映射
- `predicted_close_price` 和 `predicted_change_pct` 字段正确提取
- 数据类型转换正确（使用 `float()` 转换数值）
- 空值处理正确（使用 `if ... else None` 处理）

### 潜在问题
⚠️ **因子数据查询**（第2537-2574行）：
- 使用 `target_date` 和 `symbol` 查询 `prediction_factors` 表
- 如果表不存在或数据不存在，会返回空字典
- 这是预期的行为（因子数据是可选的）

## 2. 数据库查询方法检查 (`get_predictions`)

### 代码位置
`utils/stock_prediction_db.py` 第337-436行

### SQL查询逻辑
```sql
SELECT * FROM stock_predictions 
WHERE symbol = %s 
ORDER BY prediction_time DESC
LIMIT 1000
```

### 安全检查
✅ **SQL注入防护**：
- `order_by` 参数使用白名单验证（第380-394行）
- `limit` 参数使用类型验证和范围限制（第402-412行）
- 所有参数都使用参数化查询

### 字段支持
✅ **动态字段检测**：
- 支持 `prediction_type` 字段（可选）
- 如果字段不存在，查询仍然正常执行（使用 `SELECT *`）

### 返回数据格式
✅ **正确**：
- 返回字典列表
- 文件路径转换为完整路径
- 所有数据库字段都被包含

## 3. 前端页面检查 (`history.html`)

### 数据加载
- 调用 `/api/history?symbol=XXX` API
- 使用 `async/await` 异步加载
- 错误处理完善

### 数据渲染
✅ **字段显示**：
- `predicted_close_price` 和 `predicted_change_pct` 正确显示（第342-346行）
- 所有字段都有正确的格式化和样式
- 空值处理正确（显示 `-`）

### 详情展开
✅ **详情内容**：
- 包含完整的预测信息
- 因子数据正确显示
- 样式和交互正常

## 4. 数据库字段检查

### 必需字段
以下字段必须存在于 `stock_predictions` 表：
- `symbol`, `name`, `prediction_date`, `target_date`, `prediction_time`
- `current_price`, `prediction`, `up_probability`, `down_probability`, `confidence`, `final_score`
- `actual_price`, `actual_change_pct`, `actual_direction`, `prediction_hit`

### 可选字段（新功能）
- `prediction_type` - 预测类型（after_close/before_close）
- `predicted_close_price` - 预测收盘价
- `predicted_change_pct` - 预测涨跌幅百分比

### 字段兼容性
✅ **向后兼容**：
- 代码使用 `SELECT *`，自动包含所有字段
- 新字段的提取使用 `if ... else None`，兼容旧数据
- 前端使用可选链和默认值，兼容缺失字段

## 5. 数据格式检查

### API返回格式
```json
{
  "success": true,
  "data": {
    "symbol": "600519",
    "name": "股票名称",
    "records": [
      {
        "id": 1,
        "symbol": "600519",
        "name": "股票名称",
        "prediction_date": "2026-01-13",
        "target_date": "2026-01-14",
        "prediction_time": "2026-01-13 10:00:00",
        "current_price": 100.00,
        "predicted_close_price": 102.50,
        "predicted_change_pct": 2.50,
        "prediction": "上涨",
        "up_probability": 0.75,
        "down_probability": 0.25,
        "confidence": 0.85,
        "final_score": 0.65,
        "actual_price": 103.00,
        "actual_change_pct": 3.00,
        "actual_direction": "上涨",
        "prediction_hit": "命中",
        "factors": {}
      }
    ]
  }
}
```

✅ **格式正确**：
- 所有字段类型正确
- 数值字段使用 `float()` 转换
- 日期字段使用 `str()` 转换
- 空值使用 `None` 表示

## 6. 总结

### ✅ 正确的部分
1. **API端点逻辑正确**：
   - 参数验证完整
   - 数据库查询正确
   - 数据转换逻辑正确
   - 错误处理完善

2. **数据库查询方法安全**：
   - SQL注入防护完善
   - 参数验证严格
   - 字段兼容性好

3. **前端渲染正确**：
   - 字段显示完整
   - 格式化和样式正确
   - 错误处理完善

4. **字段映射正确**：
   - 所有字段正确映射
   - 新字段（`predicted_close_price`, `predicted_change_pct`）正确支持
   - 向后兼容性好

### ⚠️ 注意事项
1. **因子数据查询**：
   - 从 `prediction_factors` 表关联查询
   - 如果表不存在或数据不存在，返回空字典
   - 这是预期的行为，不影响主要功能

2. **数据更新**：
   - 每次请求前会更新实际数据
   - 如果更新失败，不影响数据获取
   - 可能导致实际数据不是最新的

### 结论
✅ **历史预测功能的数据获取逻辑正确，无需修改**

所有代码逻辑都正确，字段映射完整，数据格式正确，安全性良好。代码能够：
- 正确从数据库查询历史预测数据
- 正确映射所有字段（包括新字段）
- 正确处理数据格式和类型转换
- 正确返回API响应
- 正确在前端渲染数据

如果数据库中有预测数据，功能将正常工作。
