# 模型学习系统分析与设计

## 一、当前系统分析

### 1.1 预测模型现状

#### 模型结构
- **多因子模型**：结合8个主要因子
  - 新闻情感 (25%)
  - 资金流向 (18%)
  - 市场情绪 (17%)
  - 技术指标 (20%)
  - 板块轮动 (5%)
  - 历史模式 (8%)
  - 美股板块 (5%)
  - 估值指标 (2%)

#### 参数配置
- **权重参数**：存储在数据库 `prediction_config` 表中，支持动态调整
- **技术指标参数**：MA、RSI、MACD、KDJ、CCI等周期参数
- **交易决策参数**：买入/卖出阈值、止损止盈、仓位控制等

#### 数据存储
- **预测结果**：`stock_predictions` 表
  - 包含预测值和实际值（actual_price, actual_change_pct, prediction_hit）
  - 支持历史预测准确率评估
- **因子数据**：`prediction_factors` 表
  - 存储各因子的得分、权重、趋势
- **实时决策**：`realtime_trading_decisions` 表
  - 存储每次实时交易决策
  - 包含决策理由、风险警告等

### 1.2 实时交易策略现状

#### 决策逻辑
- 基于预测结果 + 实时数据（资金流向、买卖盘、盘中涨跌）
- 考虑持仓成本、止损止盈、仓位控制
- 输出：买入/卖出/加仓/减仓/持有/观望/避免

#### 参数配置
- 买入/卖出信号阈值
- 置信度要求
- 实时数据权重
- 止损止盈比例
- 仓位控制参数

### 1.3 现有分析工具

- `analysis/label_realtime_outcomes.py`：为实时决策打标签
- `analysis/review_signal_quality.py`：信号质量评估

### 1.4 存在的问题

1. **缺少自动化学习机制**
   - 权重参数需要手动调整
   - 没有基于历史表现自动优化参数

2. **缺少模型性能评估**
   - 虽然有历史数据，但没有自动化的性能评估系统
   - 无法量化模型在不同市场条件下的表现

3. **缺少参数优化算法**
   - 没有使用机器学习方法优化权重
   - 没有基于回测结果调整参数

4. **缺少定时学习任务**
   - 没有定期回测和参数更新机制

## 二、模型学习系统设计

### 2.1 系统架构

```
┌─────────────────────────────────────────────────────────┐
│                   模型学习系统                            │
├─────────────────────────────────────────────────────────┤
│                                                          │
│  ┌──────────────┐    ┌──────────────┐    ┌──────────┐ │
│  │ 性能评估模块  │ -> │ 参数优化模块  │ -> │ 参数更新  │ │
│  └──────────────┘    └──────────────┘    └──────────┘ │
│         ↑                      ↑                        │
│         │                      │                        │
│  ┌──────────────┐    ┌──────────────┐                  │
│  │ 数据收集模块  │    │ 回测模块      │                  │
│  └──────────────┘    └──────────────┘                  │
│                                                          │
│  ┌──────────────────────────────────────────────┐     │
│  │           定时任务调度器                        │     │
│  └──────────────────────────────────────────────┘     │
└─────────────────────────────────────────────────────────┘
```

### 2.2 核心模块设计

#### 2.2.1 性能评估模块 (`utils/model_performance_evaluator.py`)

**功能**：
- 评估预测模型的准确率、精确率、召回率
- 评估实时交易决策的收益率
- 分析不同市场条件下的表现
- 计算因子贡献度

**评估指标**：
1. **预测准确率**：
   - 方向准确率（上涨/下跌预测是否正确）
   - 幅度准确率（预测涨跌幅与实际涨跌幅的差异）
   - 置信度校准（高置信度预测是否真的更准确）

2. **交易决策收益**：
   - 买入信号的平均收益率
   - 卖出信号的避损效果
   - 不同置信度下的收益分布

3. **因子贡献度**：
   - 各因子对预测准确率的贡献
   - 不同市场条件下因子的有效性

#### 2.2.2 参数优化模块 (`utils/model_optimizer.py`)

**功能**：
- 使用优化算法（如网格搜索、贝叶斯优化、遗传算法）优化权重参数
- 基于历史回测结果调整参数
- 考虑不同市场状态的参数组合

**优化方法**：
1. **网格搜索**：遍历参数空间，找到最优组合
2. **贝叶斯优化**：使用高斯过程模型，高效搜索最优参数
3. **遗传算法**：模拟自然选择，进化出最优参数
4. **强化学习**：根据历史表现，学习最优权重策略

**优化目标**：
- 最大化预测准确率
- 最大化交易决策收益率
- 最小化风险（波动率、最大回撤）

#### 2.2.3 回测模块 (`utils/backtest_engine.py`)

**功能**：
- 使用历史数据模拟交易
- 评估不同参数组合的表现
- 计算收益、风险指标

**回测指标**：
- 总收益率
- 年化收益率
- 夏普比率
- 最大回撤
- 胜率
- 盈亏比

#### 2.2.4 参数更新模块 (`utils/model_parameter_updater.py`)

**功能**：
- 将优化后的参数更新到数据库
- 支持A/B测试（对比新旧参数的表现）
- 参数版本管理

### 2.3 数据库设计

#### 2.3.1 模型性能记录表 (`model_performance`)

```sql
CREATE TABLE IF NOT EXISTS `model_performance` (
    `id` BIGINT AUTO_INCREMENT PRIMARY KEY,
    `evaluation_date` DATE NOT NULL COMMENT '评估日期',
    `evaluation_type` VARCHAR(50) NOT NULL COMMENT '评估类型（prediction/trading）',
    `time_period` VARCHAR(50) DEFAULT NULL COMMENT '评估时间周期（7d/30d/90d）',
    `direction_accuracy` DECIMAL(5, 4) DEFAULT NULL COMMENT '方向准确率',
    `magnitude_mae` DECIMAL(8, 4) DEFAULT NULL COMMENT '幅度平均绝对误差',
    `confidence_calibration` DECIMAL(5, 4) DEFAULT NULL COMMENT '置信度校准度',
    `total_return` DECIMAL(8, 4) DEFAULT NULL COMMENT '总收益率',
    `sharpe_ratio` DECIMAL(8, 4) DEFAULT NULL COMMENT '夏普比率',
    `max_drawdown` DECIMAL(8, 4) DEFAULT NULL COMMENT '最大回撤',
    `win_rate` DECIMAL(5, 4) DEFAULT NULL COMMENT '胜率',
    `profit_loss_ratio` DECIMAL(8, 4) DEFAULT NULL COMMENT '盈亏比',
    `factor_contributions` JSON DEFAULT NULL COMMENT '因子贡献度（JSON）',
    `market_condition` VARCHAR(50) DEFAULT NULL COMMENT '市场状态（bull/bear/sideways）',
    `created_at` TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    INDEX `idx_evaluation_date` (`evaluation_date`),
    INDEX `idx_evaluation_type` (`evaluation_type`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='模型性能记录表';
```

#### 2.3.2 参数优化历史表 (`parameter_optimization_history`)

```sql
CREATE TABLE IF NOT EXISTS `parameter_optimization_history` (
    `id` BIGINT AUTO_INCREMENT PRIMARY KEY,
    `optimization_date` DATETIME NOT NULL COMMENT '优化日期',
    `optimization_method` VARCHAR(50) NOT NULL COMMENT '优化方法（grid_search/bayesian/genetic/rl）',
    `old_parameters` JSON DEFAULT NULL COMMENT '优化前参数（JSON）',
    `new_parameters` JSON DEFAULT NULL COMMENT '优化后参数（JSON）',
    `old_performance` JSON DEFAULT NULL COMMENT '优化前性能指标（JSON）',
    `new_performance` JSON DEFAULT NULL COMMENT '优化后性能指标（JSON）',
    `improvement_pct` DECIMAL(8, 4) DEFAULT NULL COMMENT '改进百分比',
    `is_applied` TINYINT(1) DEFAULT 0 COMMENT '是否已应用（1:是, 0:否）',
    `applied_at` DATETIME DEFAULT NULL COMMENT '应用时间',
    `backtest_result` JSON DEFAULT NULL COMMENT '回测结果（JSON）',
    `created_at` TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    INDEX `idx_optimization_date` (`optimization_date`),
    INDEX `idx_is_applied` (`is_applied`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='参数优化历史表';
```

#### 2.3.3 模型学习任务表 (`model_learning_tasks`)

```sql
CREATE TABLE IF NOT EXISTS `model_learning_tasks` (
    `id` INT AUTO_INCREMENT PRIMARY KEY,
    `task_name` VARCHAR(100) NOT NULL COMMENT '任务名称',
    `task_type` VARCHAR(50) NOT NULL COMMENT '任务类型（evaluation/optimization/backtest）',
    `schedule_type` VARCHAR(50) NOT NULL COMMENT '调度类型（daily/weekly/monthly）',
    `schedule_time` VARCHAR(20) DEFAULT NULL COMMENT '调度时间（HH:MM）',
    `is_active` TINYINT(1) DEFAULT 1 COMMENT '是否激活',
    `last_run_time` DATETIME DEFAULT NULL COMMENT '上次运行时间',
    `next_run_time` DATETIME DEFAULT NULL COMMENT '下次运行时间',
    `run_status` VARCHAR(20) DEFAULT 'pending' COMMENT '运行状态（pending/running/completed/failed）',
    `config` JSON DEFAULT NULL COMMENT '任务配置（JSON）',
    `created_at` TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    `updated_at` TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX `idx_is_active` (`is_active`),
    INDEX `idx_next_run_time` (`next_run_time`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='模型学习任务表';
```

### 2.4 定时学习任务设计

#### 2.4.1 每日性能评估任务

**触发时间**：每天收盘后（15:30）
**功能**：
- 评估过去7天、30天、90天的预测准确率
- 评估实时交易决策的收益率
- 记录性能指标到数据库

#### 2.4.2 每周参数优化任务

**触发时间**：每周日晚上（20:00）
**功能**：
- 使用过去30天的数据回测
- 优化权重参数
- 对比新旧参数的表现
- 如果新参数表现更好，自动更新

#### 2.4.3 每月深度回测任务

**触发时间**：每月最后一天（22:00）
**功能**：
- 使用过去90天的数据进行深度回测
- 评估不同市场条件下的参数表现
- 生成详细的性能报告

### 2.5 实现计划

#### 阶段1：基础功能（当前）
- [x] 数据库表设计
- [ ] 性能评估模块
- [ ] 回测模块
- [ ] 参数优化模块（基础版：网格搜索）

#### 阶段2：优化算法（后续）
- [ ] 贝叶斯优化
- [ ] 遗传算法
- [ ] 强化学习

#### 阶段3：定时任务（后续）
- [ ] 定时任务调度器
- [ ] 每日性能评估
- [ ] 每周参数优化
- [ ] 每月深度回测

## 三、预期效果

1. **自动化参数优化**：系统自动根据历史表现优化参数，无需手动调整
2. **持续改进**：定期评估和优化，模型性能持续提升
3. **量化评估**：通过数据量化模型表现，识别改进方向
4. **风险控制**：通过回测评估参数风险，避免过度优化

## 四、注意事项

1. **避免过度拟合**：使用滚动窗口回测，避免在历史数据上过度拟合
2. **参数稳定性**：参数变化不应过于频繁，避免模型不稳定
3. **A/B测试**：新参数应先进行A/B测试，确认改进后再全面应用
4. **人工审核**：重要参数变更应经过人工审核
