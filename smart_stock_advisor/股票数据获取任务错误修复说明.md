# 股票数据获取任务错误修复说明

## 一、问题描述

定时任务执行时出现以下问题：
1. **所有股票收集失败**：成功 0, 失败 5470
2. **字段更新警告**：不允许更新字段或值为None: last_run_status, last_run_message, success_count, fail_count
3. **任务状态判断错误**：即使所有股票都失败了，任务状态仍然显示为 success

## 二、问题原因

### 1. 增量更新方法调用错误
- **问题**：在多线程工作函数中，增量更新时调用了错误的方法
- **错误代码**：`collector.collect_stock_history_data(symbol, start_date=today, end_date=today)`
- **原因**：`collect_stock_history_data` 方法不接受 `start_date` 和 `end_date` 参数，只接受 `years` 参数
- **正确方法**：应该使用 `collect_stock_daily_data(symbol, today)` 方法

### 2. 字段更新限制
- **问题**：尝试更新 `last_run_status`, `last_run_message`, `success_count`, `fail_count` 字段时出现警告
- **原因**：`_update_task_status` 方法的 `allowed_fields` 列表中没有包含这些字段
- **实际情况**：数据库表 `scheduled_tasks` 中确实有这些字段（根据 `scheduled_tasks_table.sql`）

### 3. 任务状态判断逻辑错误
- **问题**：即使所有股票都失败了，任务状态仍然显示为 success
- **原因**：只检查了返回字典的 `success` 字段，没有检查实际收集结果
- **影响**：无法正确反映任务执行的真实状态

## 三、修复内容

### 1. 修复增量更新方法调用（`_collect_single_cn_stock_worker`）

**修改前**：
```python
result = collector.collect_stock_history_data(symbol, start_date=today, end_date=today)
```

**修改后**：
```python
# 使用 collect_stock_daily_data 方法采集单日数据
data = collector.collect_stock_daily_data(symbol, today)
if data:
    # 保存数据
    if collector.storage.save_stock_daily_data(symbol, today, data):
        return {
            'symbol': symbol,
            'success': True,
            'message': '今日数据更新成功',
            'records_count': 1,
            'skipped': False
        }
    else:
        return {
            'symbol': symbol,
            'success': False,
            'message': '今日数据保存失败',
            'records_count': 0,
            'skipped': False
        }
else:
    return {
        'symbol': symbol,
        'success': False,
        'message': '今日数据采集失败',
        'records_count': 0,
        'skipped': False
    }
```

### 2. 扩展允许更新的字段列表（`_update_task_status`）

**修改前**：
```python
allowed_fields = {
    'status', 'next_run_time', 'last_run_time', 
    'run_count', 'error_count', 'last_error', 
    'is_enabled', 'last_result'
}
```

**修改后**：
```python
allowed_fields = {
    'status', 'next_run_time', 'last_run_time', 
    'run_count', 'error_count', 'last_error', 
    'is_enabled', 'last_result',
    'last_run_status', 'last_run_message',  # 如果表中有这些字段
    'success_count', 'fail_count'  # 如果表中有这些字段
}
```

**改进**：
- 将警告级别从 `warning` 降低到 `debug`（因为可能是可选字段）
- 如果值为 `None`，不记录警告

### 3. 改进任务状态判断逻辑（`_execute_task`）

**修改前**：
```python
if result and result.get('success', False):
    status = 'success'
    message = result.get('message', '执行成功')
else:
    status = 'failed'
    message = result.get('message', '执行失败') if result else '执行失败'
```

**修改后**：
```python
# 对于股票数据收集任务，需要根据实际收集结果判断状态
if task_type in ['stock_data_collection_cn', 'stock_data_collection_us']:
    if result and result.get('success', False):
        # 检查实际收集结果
        data = result.get('data', {})
        success_count = data.get('success_count', 0)
        fail_count = data.get('fail_count', 0)
        total_count = success_count + fail_count
        
        # 如果全部失败，标记为失败
        if total_count > 0 and success_count == 0:
            status = 'failed'
            message = f"所有股票收集失败: {fail_count} 只股票失败"
            error_info = result.get('message', '所有股票收集失败')
        # 如果有部分成功，保持成功状态但更新消息
        elif success_count > 0:
            status = 'success'
            message = result.get('message', '执行成功')
            error_info = ''
        else:
            # 没有数据，可能是配置问题
            status = 'failed'
            message = result.get('message', '没有需要处理的股票')
            error_info = result.get('message', '没有需要处理的股票')
    else:
        status = 'failed'
        message = result.get('message', '执行失败') if result else '执行失败'
        error_info = result.get('error', '') if result else ''
```

### 4. 改进字段更新逻辑（`_execute_task`）

**修改前**：
```python
self._update_task_status(task_id, last_run_time=start_time, last_run_status=status,
                       last_run_message=message, run_count=task.get('run_count', 0) + 1,
                       success_count=task.get('success_count', 0) + (1 if status == 'success' else 0),
                       fail_count=task.get('fail_count', 0) + (1 if status == 'failed' else 0))
```

**修改后**：
```python
# 更新任务状态（只更新允许的字段）
update_kwargs = {
    'last_run_time': start_time.strftime('%Y-%m-%d %H:%M:%S'),
    'run_count': task.get('run_count', 0) + 1
}

# 如果数据库表有这些字段，尝试更新（通过检查表结构）
try:
    # 检查字段是否存在
    sql_check = "SHOW COLUMNS FROM scheduled_tasks LIKE %s"
    if self.db.execute_query(sql_check, ('last_run_status',)):
        update_kwargs['last_run_status'] = status
    if self.db.execute_query(sql_check, ('last_run_message',)):
        update_kwargs['last_run_message'] = message[:500] if message else None  # 限制长度
    if self.db.execute_query(sql_check, ('success_count',)):
        update_kwargs['success_count'] = task.get('success_count', 0) + (1 if status == 'success' else 0)
    if self.db.execute_query(sql_check, ('fail_count',)):
        update_kwargs['fail_count'] = task.get('fail_count', 0) + (1 if status == 'failed' else 0)
except Exception as e:
    self.logger.debug(f"检查字段存在性失败: {str(e)}")

self._update_task_status(task_id, **update_kwargs)
```

## 四、修复效果

### 1. 增量更新修复
- ✅ 使用正确的方法 `collect_stock_daily_data` 采集单日数据
- ✅ 正确处理数据保存结果
- ✅ 返回正确的成功/失败状态

### 2. 字段更新修复
- ✅ 扩展允许更新的字段列表
- ✅ 动态检查字段是否存在（兼容不同表结构）
- ✅ 减少不必要的警告信息

### 3. 任务状态判断修复
- ✅ 根据实际收集结果判断任务状态
- ✅ 如果全部失败，正确标记为 failed
- ✅ 如果有部分成功，保持 success 状态但更新消息

## 五、相关文件

- `utils/scheduled_task_manager.py`：
  - `_collect_single_cn_stock_worker()` - 修复增量更新方法调用（第601-640行）
  - `_execute_task()` - 改进任务状态判断逻辑（第348-415行）
  - `_update_task_status()` - 扩展允许更新的字段列表（第1358-1383行）

## 六、测试建议

1. **测试增量更新**：
   - 创建增量更新任务
   - 验证今日数据是否正确采集和保存
   - 验证重复数据是否正确跳过

2. **测试任务状态**：
   - 创建全量收集任务（使用不存在的股票代码）
   - 验证任务状态是否正确标记为 failed
   - 验证错误消息是否正确显示

3. **测试字段更新**：
   - 验证 `last_run_status`, `last_run_message`, `success_count`, `fail_count` 字段是否正确更新
   - 验证不再出现字段更新警告

## 七、完成时间

2026-01-14
