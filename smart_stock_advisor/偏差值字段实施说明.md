# 偏差值字段实施说明

**日期**: 2026-01-16  
**状态**: ✅ 已完成

---

## 一、实施步骤

### 步骤1: 执行数据库迁移

**执行SQL脚本**:
```bash
# 方式1: 使用mysql命令行
mysql -u username -p database_name < database/add_deviation_fields.sql

# 方式2: 在MySQL客户端中执行
source database/add_deviation_fields.sql;
```

**脚本内容**:
- 添加 `deviation_pct` 字段（偏差值，有符号）
- 添加 `absolute_deviation_pct` 字段（绝对偏差值）
- 添加 `deviation_price` 字段（价格偏差）
- 添加索引
- 为已有数据填充偏差值

---

### 步骤2: 填充历史数据（可选）

**如果字段已添加但历史数据未填充，执行**:
```bash
mysql -u username -p database_name < database/fill_historical_deviation.sql
```

**脚本功能**:
- 为已有数据计算并填充偏差值
- 只更新为NULL的记录，避免覆盖已有数据
- 显示更新统计

---

### 步骤3: 验证实施

**验证字段是否添加成功**:
```sql
SHOW COLUMNS FROM stock_predictions LIKE 'deviation%';
```

**验证数据是否填充**:
```sql
SELECT 
    COUNT(*) as total,
    SUM(CASE WHEN deviation_pct IS NOT NULL THEN 1 ELSE 0 END) as with_deviation
FROM stock_predictions
WHERE actual_change_pct IS NOT NULL;
```

---

## 二、代码修改说明

### 2.1 stock_prediction_db.py

**修改内容**:
1. ✅ 在保存预测结果时计算偏差值（第103-119行）
2. ✅ 检查偏差值字段是否存在（第211-212行）
3. ✅ 在INSERT语句中添加偏差值字段（第318-326行）

**自动计算逻辑**:
- 如果 `predicted_change_pct` 和 `actual_change_pct` 都存在，自动计算偏差值
- 如果 `predicted_close_price` 和 `actual_price` 都存在，自动计算价格偏差
- 新预测记录保存时，如果实际值已存在，会自动计算并存储偏差值

---

### 2.2 model_performance_evaluator.py

**修改内容**:
1. ✅ 在SQL查询中添加偏差值字段（第44-59行）
2. ✅ 优化MAE计算，优先使用存储的偏差值（第76-102行）

**优化效果**:
- 优先使用 `absolute_deviation_pct`（如果存在）
- 其次使用 `deviation_pct`（如果存在）
- 再次使用 `predicted_change_pct` 和 `actual_change_pct` 计算
- 最后才使用简化的估算方法（向后兼容）

---

## 三、使用示例

### 3.1 查询偏差值

**查询偏差值最大的10条预测**:
```sql
SELECT symbol, target_date, 
       predicted_change_pct, actual_change_pct,
       deviation_pct, absolute_deviation_pct
FROM stock_predictions
WHERE actual_change_pct IS NOT NULL
ORDER BY absolute_deviation_pct DESC
LIMIT 10;
```

**统计偏差分布**:
```sql
SELECT 
    AVG(deviation_pct) as avg_deviation,
    STDDEV(deviation_pct) as std_deviation,
    MIN(deviation_pct) as min_deviation,
    MAX(deviation_pct) as max_deviation,
    AVG(absolute_deviation_pct) as avg_absolute_deviation
FROM stock_predictions
WHERE deviation_pct IS NOT NULL;
```

---

### 3.2 自动计算

**新预测记录**:
- ✅ 保存预测结果时，如果实际值已存在，自动计算并存储偏差值
- ✅ 如果实际值后更新，偏差值也会自动更新（通过UPDATE语句）

**历史数据**:
- ✅ 执行 `fill_historical_deviation.sql` 脚本即可填充

---

## 四、注意事项

### 4.1 字段兼容性

- ✅ 代码会自动检测字段是否存在
- ✅ 如果字段不存在，不会报错，只是不保存偏差值
- ✅ 向后兼容，不影响现有功能

### 4.2 数据更新

- ✅ 新预测记录：自动计算并存储偏差值
- ✅ 历史数据：需要执行填充脚本
- ✅ 实际值更新：偏差值会自动更新（通过ON DUPLICATE KEY UPDATE）

---

## 五、后续优化建议

### 5.1 模型学习优化

在参数优化时考虑偏差值：
```python
# 在model_optimizer.py中
def evaluate_parameters(self, params):
    # 计算方向准确率
    direction_accuracy = ...
    
    # 计算偏差指标（MAE）
    deviation_mae = self._calculate_deviation_mae(params)
    
    # 综合评估（方向准确率 + 偏差指标）
    score = direction_accuracy * 0.6 + (1 - deviation_mae / 10.0) * 0.4
    return score
```

### 5.2 模型训练优化

使用偏差值作为特征或标签：
```python
# 在ml_data_loader.py中
# 作为特征：添加历史偏差统计
features['avg_deviation_30d'] = historical_avg_deviation
features['std_deviation_30d'] = historical_std_deviation

# 作为标签：训练偏差预测模型
label = deviation_pct  # 预测偏差值
```

---

**文档生成时间**: 2026-01-16  
**状态**: ✅ 所有功能已实施完成
