# 数据缺失处理策略分析与改进方案

## 一、当前问题分析

### 1.1 现有处理方式的问题

**问题1：使用0.0作为默认得分不够准确**
- **现状**：缺失字段时，各因子得分默认为0.0
- **问题**：0.0可能被解释为"中性"，但实际上应该是"数据缺失，无法评估"
- **影响**：可能导致预测结果偏向中性，降低准确性

**问题2：权重未根据数据完整性动态调整**
- **现状**：即使数据缺失，权重仍然保持不变
- **问题**：缺失数据的因子仍然占据相同权重，影响预测准确性
- **影响**：数据质量差的预测与数据质量好的预测权重相同

**问题3：置信度计算未充分考虑数据缺失**
- **现状**：置信度计算中有数据质量评估，但可能不够完善
- **问题**：数据缺失时，置信度应该显著降低
- **影响**：用户可能对低质量预测结果过度信任

**问题4：缺失字段的默认值不够智能**
- **现状**：所有缺失字段都使用0.0或None
- **问题**：某些因子应该使用中性值（如0.0），某些应该使用保守值
- **影响**：预测结果可能不够准确

## 二、改进策略

### 2.1 数据完整性评估

**为每个因子计算数据完整性评分**：

```python
def _calculate_factor_data_quality(self, factor_name: str, factor_result: Dict) -> float:
    """
    计算因子数据质量评分（0-1）
    
    Args:
        factor_name: 因子名称
        factor_result: 因子分析结果
    
    Returns:
        数据质量评分（0-1），1表示数据完整，0表示数据完全缺失
    """
    quality_score = 1.0
    
    if factor_name == 'technical':
        # 技术指标：检查是否有足够的历史数据
        # 如果数据不足，降低质量评分
        signals = factor_result.get('signals', {})
        if not signals or len(signals) < 3:
            quality_score = 0.5  # 数据不完整
        if factor_result.get('score') == 0.0 and not signals:
            quality_score = 0.3  # 可能是默认值，数据缺失
    
    elif factor_name == 'news':
        # 新闻：检查新闻数量
        news_count = factor_result.get('news_count', 0)
        if news_count == 0:
            quality_score = 0.2  # 无新闻数据
        elif news_count < 3:
            quality_score = 0.6  # 新闻数量较少
    
    elif factor_name == 'capital_flow':
        # 资金流向：检查数据完整性
        details = factor_result.get('details', {})
        available_sources = sum(1 for k in ['north_bound', 'margin', 'main_force'] if k in details)
        quality_score = available_sources / 3.0  # 3个数据源
    
    elif factor_name == 'valuation':
        # 估值：检查PE/PB是否可用
        pe_ratio = factor_result.get('pe_ratio')
        pb_ratio = factor_result.get('pb_ratio')
        if pe_ratio is None and pb_ratio is None:
            quality_score = 0.0  # 完全缺失
        elif pe_ratio is None or pb_ratio is None:
            quality_score = 0.5  # 部分缺失
    
    elif factor_name == 'market':
        # 市场情绪：检查数据源
        details = factor_result.get('details', {})
        if 'market_index_score' not in details and 'stock_score' not in details:
            quality_score = 0.3  # 数据缺失
    
    elif factor_name == 'history':
        # 历史模式：检查是否有模式识别
        pattern = factor_result.get('pattern', 'unknown')
        if pattern == 'unknown':
            quality_score = 0.2  # 无历史模式
    
    return quality_score
```

### 2.2 动态权重调整

**根据数据完整性动态调整权重**：

```python
def _adjust_weights_by_data_quality(self, base_weights: Dict, factor_quality_scores: Dict) -> Dict:
    """
    根据数据质量动态调整权重
    
    Args:
        base_weights: 基础权重
        factor_quality_scores: 各因子数据质量评分
    
    Returns:
        调整后的权重
    """
    adjusted_weights = {}
    total_adjusted_weight = 0.0
    
    # 计算调整后的权重（质量评分作为权重倍数）
    for factor_name, base_weight in base_weights.items():
        quality_score = factor_quality_scores.get(factor_name, 1.0)
        # 如果质量评分低于0.3，大幅降低权重
        if quality_score < 0.3:
            adjusted_weight = base_weight * quality_score * 0.5  # 额外降低50%
        elif quality_score < 0.6:
            adjusted_weight = base_weight * quality_score * 0.8  # 降低20%
        else:
            adjusted_weight = base_weight * quality_score
        
        adjusted_weights[factor_name] = adjusted_weight
        total_adjusted_weight += adjusted_weight
    
    # 归一化权重（确保总和为1）
    if total_adjusted_weight > 0:
        for factor_name in adjusted_weights:
            adjusted_weights[factor_name] /= total_adjusted_weight
    else:
        # 如果所有权重都为0，回退到基础权重
        adjusted_weights = base_weights
    
    return adjusted_weights
```

### 2.3 智能默认值策略

**根据因子特性使用不同的默认值**：

```python
def _get_default_factor_score(self, factor_name: str) -> float:
    """
    获取因子的智能默认值
    
    Args:
        factor_name: 因子名称
    
    Returns:
        默认得分
    """
    # 中性默认值（0.0）：适用于大多数因子
    neutral_defaults = ['technical', 'market', 'history', 'sector_rotation']
    
    # 保守默认值（轻微负值）：适用于不确定性较高的因子
    conservative_defaults = {
        'news': 0.0,  # 无新闻时，保持中性
        'capital_flow': -0.05,  # 资金流向不明时，略微保守
        'valuation': 0.0,  # 估值不明时，保持中性
        'us_sector': 0.0  # 美股板块不明时，保持中性
    }
    
    if factor_name in neutral_defaults:
        return 0.0
    elif factor_name in conservative_defaults:
        return conservative_defaults[factor_name]
    else:
        return 0.0  # 默认中性
```

### 2.4 置信度调整

**根据数据完整性调整置信度**：

```python
def _adjust_confidence_by_data_quality(self, base_confidence: float, 
                                       factor_quality_scores: Dict,
                                       missing_critical_data: bool = False) -> float:
    """
    根据数据质量调整置信度
    
    Args:
        base_confidence: 基础置信度
        factor_quality_scores: 各因子数据质量评分
        missing_critical_data: 是否缺失关键数据
    
    Returns:
        调整后的置信度
    """
    # 如果缺失关键数据，大幅降低置信度
    if missing_critical_data:
        return base_confidence * 0.3
    
    # 计算平均数据质量评分
    if factor_quality_scores:
        avg_quality = sum(factor_quality_scores.values()) / len(factor_quality_scores)
    else:
        avg_quality = 1.0
    
    # 根据平均质量调整置信度
    # 质量评分 < 0.5：降低50%
    # 质量评分 < 0.7：降低30%
    # 质量评分 >= 0.7：降低10%
    if avg_quality < 0.5:
        quality_adjustment = 0.5
    elif avg_quality < 0.7:
        quality_adjustment = 0.7
    else:
        quality_adjustment = 0.9
    
    adjusted_confidence = base_confidence * quality_adjustment
    
    # 确保置信度不低于0.1（除非数据完全缺失）
    if avg_quality > 0:
        adjusted_confidence = max(0.1, adjusted_confidence)
    else:
        adjusted_confidence = 0.05  # 数据完全缺失时，极低置信度
    
    return adjusted_confidence
```

### 2.5 数据质量信息传递

**在预测结果中包含数据质量信息**：

```python
# 在预测结果中添加数据质量信息
result['data_quality'] = {
    'overall_quality': avg_quality,
    'factor_quality': factor_quality_scores,
    'missing_factors': [name for name, score in factor_quality_scores.items() if score < 0.3],
    'quality_warning': avg_quality < 0.5
}
```

## 三、实施建议

### 3.1 优先级排序

**高优先级**（立即实施）：
1. **数据完整性评估**：为每个因子计算数据质量评分
2. **动态权重调整**：根据数据质量降低缺失数据的权重
3. **置信度调整**：数据缺失时降低置信度

**中优先级**（后续优化）：
4. **智能默认值**：根据因子特性使用不同的默认值
5. **数据质量信息传递**：在预测结果中包含数据质量信息

### 3.2 实施步骤

**第一步：添加数据质量评估方法**
- 在 `StockPredictor` 类中添加 `_calculate_factor_data_quality()` 方法
- 为每个因子计算数据质量评分

**第二步：修改权重调整逻辑**
- 在 `_process_prediction_results_internal()` 方法中
- 调用 `_adjust_weights_by_data_quality()` 调整权重
- 确保权重归一化

**第三步：修改置信度计算**
- 在 `_calculate_confidence()` 方法中
- 调用 `_adjust_confidence_by_data_quality()` 调整置信度
- 传递数据质量信息

**第四步：添加数据质量信息到结果**
- 在预测结果字典中添加 `data_quality` 字段
- 包含整体质量评分和各因子质量评分

### 3.3 预期效果

**准确性提升**：
- 数据缺失时，预测结果更保守（降低权重和置信度）
- 数据完整时，预测结果更可靠（保持或提高权重和置信度）

**用户体验提升**：
- 用户可以看到数据质量信息
- 低质量预测的置信度更低，用户不会过度信任

**干扰减少**：
- 数据缺失时，自动降低影响，不会产生误导性预测
- 预测结果更准确，减少不必要的干扰

## 四、代码修改位置

1. **`predictor/stock_predictor.py`**：
   - 添加 `_calculate_factor_data_quality()` 方法
   - 添加 `_adjust_weights_by_data_quality()` 方法
   - 添加 `_get_default_factor_score()` 方法
   - 添加 `_adjust_confidence_by_data_quality()` 方法
   - 修改 `_process_prediction_results_internal()` 方法，集成数据质量评估
   - 修改 `_calculate_confidence()` 方法，使用数据质量调整

## 五、测试建议

1. **数据完整性测试**：
   - 测试各种数据缺失场景
   - 验证数据质量评分是否正确

2. **权重调整测试**：
   - 验证缺失数据的权重是否降低
   - 验证权重归一化是否正确

3. **置信度调整测试**：
   - 验证数据缺失时置信度是否降低
   - 验证置信度范围是否合理

4. **准确性测试**：
   - 对比优化前后的预测准确性
   - 验证数据缺失时的预测是否更保守

---

**分析完成时间**：2026-01-26  
**改进状态**：📋 待实施  
**预期效果**：提高预测准确性，减少数据缺失时的干扰
