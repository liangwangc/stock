# "未收盘-明天"预测价格保存检查报告

## 📋 检查目标

检查主页"股票预测列表（未收盘-明天）"页签中的预测是否保存了：
- ✅ `predicted_close_price`（明日预测收盘价）
- ✅ `predicted_change_pct`（明日预测涨幅）

## 🔍 代码流程分析

### 1. 预测触发流程

**API端点**：`/api/predict/before_close` (POST)
- **位置**：`web_app.py` 第4323行
- **功能**：处理"未收盘-明天"预测请求

**关键代码**：
```python
# 第4375行：执行预测
prediction_result = predictor.predict(symbol)

# 第4388行：设置预测类型
prediction_result['prediction_type'] = 'before_close'

# 第4391行：保存预测结果
visualizer.save_stock_record(symbol, prediction_result)
```

### 2. 预测计算流程

**文件**：`predictor/stock_predictor.py`

**计算预测价格和涨幅**（第2796-2798行）：
```python
predicted_change_pct = np.tanh(probability_diff * 3) * max_change_pct * confidence
predicted_close_price = current_price * (1 + predicted_change_pct / 100.0)
```

**返回结果**（第2983-2984行）：
```python
'predicted_close_price': predicted_close_price,  # 明日大概收盘价格
'predicted_change_pct': predicted_change_pct,  # 明日大概涨幅百分比
```

✅ **结论**：预测时会计算并返回这两个字段

### 3. 保存流程

**文件**：`visualizer/prediction_visualizer.py`

**保存方法**：`save_stock_record()`（第1754行）
- 调用 `db.save_prediction()` 保存到数据库
- 传递完整的 `prediction_result` 字典

**文件**：`utils/stock_prediction_db.py`

**提取字段**（第60-61行）：
```python
predicted_close_price = prediction_result.get('predicted_close_price', None)
predicted_change_pct = prediction_result.get('predicted_change_pct', None)
```

**保存到数据库**（第165-166行）：
```python
predicted_close_price if predicted_close_price else None,
predicted_change_pct if predicted_change_pct is not None else None,
```

✅ **结论**：保存逻辑会提取并保存这两个字段

### 4. 数据查询流程

**API端点**：`/api/stocks` (GET)
- **参数**：`prediction_type=before_close`
- **位置**：`web_app.py` 第877行

**查询逻辑**（第901-922行）：
```python
if prediction_type == 'before_close':
    records = db.get_predictions(symbol=None, limit=1000, 
                                 order_by='prediction_time DESC', 
                                 prediction_type='before_close')
```

**返回数据**（第1065-1066行）：
```python
'predicted_close_price': float(record.get('predicted_close_price', 0)) if record.get('predicted_close_price') else None,
'predicted_change_pct': float(record.get('predicted_change_pct', 0)) if record.get('predicted_change_pct') is not None else None,
```

✅ **结论**：API会返回这两个字段

## ✅ 代码逻辑检查结果

| 步骤 | 功能 | 状态 | 说明 |
|------|------|------|------|
| 1 | 预测计算 | ✅ | `predictor.predict()` 会计算并返回这两个字段 |
| 2 | 设置类型 | ✅ | 设置 `prediction_type='before_close'` |
| 3 | 保存提取 | ✅ | `save_prediction()` 会提取这两个字段 |
| 4 | 数据库保存 | ✅ | SQL包含这两个字段的保存逻辑 |
| 5 | 数据查询 | ✅ | API会查询并返回这两个字段 |
| 6 | 前端显示 | ✅ | `stock_list.html` 会显示这两个字段 |

## ⚠️ 潜在问题

### 问题1：数据库字段不存在

**症状**：保存时字段不存在，数据无法保存

**检查方法**：
```sql
SHOW COLUMNS FROM stock_predictions LIKE 'predicted_close_price';
SHOW COLUMNS FROM stock_predictions LIKE 'predicted_change_pct';
```

**解决方法**：
```bash
python scripts/check_and_add_predicted_price_fields.py
```

### 问题2：旧数据没有这些字段的值

**症状**：旧预测记录中这两个字段为 NULL

**原因**：在添加字段之前保存的记录

**解决方法**：重新运行预测

### 问题3：保存逻辑分支问题

**位置**：`stock_prediction_db.py` 第123行

**说明**：只有当检测到字段存在时才会保存。如果字段不存在，会使用不包含这两个字段的SQL。

**检查**：确保字段存在后，所有分支都会保存这两个字段。

## 🔧 验证方法

### 方法1：运行检查脚本

```bash
python scripts/check_before_close_predictions.py
```

这个脚本会：
1. ✅ 检查字段是否存在
2. ✅ 查询 before_close 类型的记录
3. ✅ 统计有预测价格和涨幅的记录数量
4. ✅ 显示最近10条记录的详细信息

### 方法2：手动SQL查询

```sql
-- 检查字段是否存在
SHOW COLUMNS FROM stock_predictions LIKE 'predicted_close_price';
SHOW COLUMNS FROM stock_predictions LIKE 'predicted_change_pct';

-- 查询未收盘-明天的预测记录
SELECT 
    symbol, name, 
    current_price, 
    predicted_close_price, 
    predicted_change_pct,
    prediction_time
FROM stock_predictions
WHERE prediction_type = 'before_close'
ORDER BY prediction_time DESC
LIMIT 10;

-- 统计有预测价格的记录
SELECT 
    COUNT(*) as total,
    COUNT(predicted_close_price) as has_price,
    COUNT(predicted_change_pct) as has_change_pct
FROM stock_predictions
WHERE prediction_type = 'before_close';
```

### 方法3：前端检查

1. 打开主页
2. 切换到"⏰ 股票预测列表（未收盘-明天）"页签
3. 查看股票列表中是否显示"明日收盘价"和"明日预测涨幅"

## 📊 预期结果

### 正常情况

- ✅ 数据库字段存在
- ✅ 新预测记录包含 `predicted_close_price` 和 `predicted_change_pct`
- ✅ 前端页面正确显示这两个值

### 异常情况

- ❌ 字段不存在 → 运行添加字段脚本
- ❌ 旧记录为 NULL → 重新预测
- ❌ 前端不显示 → 检查前端代码

## 🎯 总结

**代码逻辑完整性**：✅ 完整

1. ✅ 预测时会计算这两个值
2. ✅ 预测结果中包含这两个值
3. ✅ 保存时会提取并保存这两个值
4. ✅ 查询时会返回这两个值
5. ✅ 前端会显示这两个值

**关键检查点**：
- 确保数据库字段存在
- 确保新预测包含这两个值
- 确保前端正确显示

## 📝 下一步操作

1. ✅ 运行 `check_before_close_predictions.py` 检查实际数据
2. ✅ 如果字段不存在，运行 `check_and_add_predicted_price_fields.py`
3. ✅ 进行一次新的"未收盘-明天"预测，验证数据保存
4. ✅ 检查前端页面是否正确显示
