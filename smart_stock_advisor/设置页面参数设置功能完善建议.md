# 设置页面参数设置功能完善建议

**日期**: 2026-01-16  
**目标**: 结合前面优化新增的功能，检查设置页面参数设置还需要增加什么内容

---

## 一、当前参数设置功能

### 1.1 已有功能

✅ **预测因子权重配置**:
- 新闻情感权重
- 资金流向权重
- 市场情绪权重
- 技术指标权重
- 板块轮动权重
- 历史模式权重
- 美股板块权重
- 估值指标权重

✅ **技术指标参数配置**:
- MA均线参数（MA5, MA10, MA20, MA60）
- RSI参数
- MACD参数
- KDJ参数
- CCI参数

✅ **其他设置**:
- 最小置信度
- 回看天数

---

## 二、新增功能但缺少配置项

### 2.1 ML模型动态权重参数 ⚠️

**功能状态**: ✅ 已实现  
**配置状态**: ❌ 未在设置页面提供配置

**当前实现位置**: `utils/ml_model_performance_monitor.py`

**当前参数（硬编码）**:
```python
_default_weight = 0.15       # 默认权重15%
_min_weight = 0.0            # 最小权重
_max_weight = 0.25           # 最大权重25%

# 权重计算阈值
_high_accuracy_threshold = 0.60    # 高准确率阈值60%
_medium_accuracy_threshold = 0.50  # 中等准确率阈值50%
```

**建议添加的配置项**:
1. **ML模型默认权重**: 0-1之间，默认0.15
2. **ML模型最小权重**: 0-1之间，默认0.0
3. **ML模型最大权重**: 0-1之间，默认0.25
4. **高准确率阈值**: 0-1之间，默认0.60（准确率>此值时使用高权重）
5. **中等准确率阈值**: 0-1之间，默认0.50（准确率>此值时使用中等权重）

**配置位置建议**: 在"预测因子权重配置"部分添加"ML模型权重配置"子区域

---

### 2.2 X2指标参数 ⚠️

**功能状态**: ✅ 已实现  
**配置状态**: ❌ 未在设置页面提供配置

**当前实现位置**: `predictor/stock_predictor.py` (calculate_technical_score方法)

**当前参数（硬编码）**:
```python
x2_overbought_threshold = 80   # 超买阈值
x2_oversold_threshold = 20     # 超卖阈值
x2_high_threshold = 60         # 偏高阈值
x2_low_threshold = 40          # 偏低阈值

x2_overbought_score = -0.15    # 超买得分
x2_oversold_score = 0.15       # 超卖得分
x2_high_score = -0.05         # 偏高得分
x2_low_score = 0.05           # 偏低得分
```

**建议添加的配置项**:
1. **X2超买阈值**: 0-100，默认80
2. **X2超卖阈值**: 0-100，默认20
3. **X2偏高阈值**: 0-100，默认60
4. **X2偏低阈值**: 0-100，默认40
5. **X2超买得分**: -1到1，默认-0.15
6. **X2超卖得分**: -1到1，默认0.15
7. **X2偏高得分**: -1到1，默认-0.05
8. **X2偏低得分**: -1到1，默认0.05

**配置位置建议**: 在"技术指标参数配置"部分添加"X2指标参数"子区域

---

### 2.3 异常检测参数 ⚠️

**功能状态**: ✅ 已实现  
**配置状态**: ❌ 未在设置页面提供配置

**当前实现位置**: `predictor/stock_predictor.py` (_process_prediction_results_internal方法)

**当前参数（硬编码）**:
```python
st_stock_confidence_reduction = 0.20      # ST股票降低20%置信度
limit_up_down_confidence_reduction = 0.10  # 涨跌停降低10%置信度
```

**建议添加的配置项**:
1. **ST股票置信度降低比例**: 0-1之间，默认0.20（20%）
2. **涨跌停置信度降低比例**: 0-1之间，默认0.10（10%）
3. **停牌股票处理方式**: 下拉选择（"跳过预测"/"降低置信度"），默认"跳过预测"

**配置位置建议**: 在"其他设置"部分添加"异常检测参数"子区域

---

### 2.4 多线程配置 ⚠️

**功能状态**: ✅ 已实现  
**配置状态**: ❌ 未在设置页面提供配置

**当前实现位置**: 
- 设置页面股票分析: `web_app.py` (BATCH_ANALYSIS_CONFIG)
- 主页未收盘预测: `web_app.py` (动态确定)

**当前参数（部分硬编码）**:
```python
# 设置页面股票分析
BATCH_ANALYSIS_CONFIG = {
    "max_workers": 3,        # 默认3个线程
    "enable_parallel": True  # 默认启用多线程
}

# 主页未收盘预测（动态确定，不可配置）
# 1只股票: 1个线程
# 2-3只股票: 使用股票数量作为线程数
# 4-5只股票: 最多3个线程
# 5只以上: 最多5个线程
```

**建议添加的配置项**:
1. **设置页面分析线程数**: 1-10，默认3
2. **设置页面是否启用多线程**: 开关，默认开启
3. **主页未收盘预测最大线程数**: 1-10，默认5（用于限制动态线程数的上限）

**配置位置建议**: 在"其他设置"部分添加"性能优化参数"子区域

---

### 2.5 实时策略缓存参数 ⚠️

**功能状态**: ✅ 已实现  
**配置状态**: ❌ 未在设置页面提供配置

**当前实现位置**: `predictor/realtime_trading_advisor.py`

**当前参数（硬编码）**:
```python
# 动态缓存时长（根据市场状态调整）
cache_duration_min = 5   # 最小缓存时长（分钟）
cache_duration_max = 15  # 最大缓存时长（分钟）

# 实时数据缓存时长
realtime_data_cache_duration = 10  # 秒
```

**建议添加的配置项**:
1. **预测结果最小缓存时长**: 1-60分钟，默认5分钟
2. **预测结果最大缓存时长**: 1-60分钟，默认15分钟
3. **实时数据缓存时长**: 1-60秒，默认10秒

**配置位置建议**: 在"其他设置"部分添加"缓存参数"子区域

---

## 三、建议添加的配置项汇总

### 3.1 ML模型动态权重配置

**位置**: "预测因子权重配置" → "ML模型权重配置"

| 配置项 | 类型 | 默认值 | 范围 | 说明 |
|--------|------|--------|------|------|
| ML模型默认权重 | 数字 | 0.15 | 0-1 | ML模型的默认权重（当无法获取性能数据时使用） |
| ML模型最小权重 | 数字 | 0.0 | 0-1 | ML模型的最小权重（性能差时使用） |
| ML模型最大权重 | 数字 | 0.25 | 0-1 | ML模型的最大权重（性能好时使用） |
| 高准确率阈值 | 数字 | 0.60 | 0-1 | 准确率>此值时使用高权重范围 |
| 中等准确率阈值 | 数字 | 0.50 | 0-1 | 准确率>此值时使用中等权重范围 |

---

### 3.2 X2指标参数配置

**位置**: "技术指标参数配置" → "X2指标参数"

| 配置项 | 类型 | 默认值 | 范围 | 说明 |
|--------|------|--------|------|------|
| X2超买阈值 | 数字 | 80 | 0-100 | X2值>此值时判定为超买 |
| X2超卖阈值 | 数字 | 20 | 0-100 | X2值<此值时判定为超卖 |
| X2偏高阈值 | 数字 | 60 | 0-100 | X2值>此值时判定为偏高 |
| X2偏低阈值 | 数字 | 40 | 0-100 | X2值<此值时判定为偏低 |
| X2超买得分 | 数字 | -0.15 | -1到1 | 超买时的得分 |
| X2超卖得分 | 数字 | 0.15 | -1到1 | 超卖时的得分 |
| X2偏高得分 | 数字 | -0.05 | -1到1 | 偏高时的得分 |
| X2偏低得分 | 数字 | 0.05 | -1到1 | 偏低时的得分 |

---

### 3.3 异常检测参数配置

**位置**: "其他设置" → "异常检测参数"

| 配置项 | 类型 | 默认值 | 范围 | 说明 |
|--------|------|--------|------|------|
| ST股票置信度降低比例 | 数字 | 0.20 | 0-1 | ST股票预测时降低的置信度比例 |
| 涨跌停置信度降低比例 | 数字 | 0.10 | 0-1 | 涨跌停股票预测时降低的置信度比例 |
| 停牌股票处理方式 | 下拉 | "跳过预测" | - | 停牌股票的处理方式（跳过预测/降低置信度） |

---

### 3.4 性能优化参数配置

**位置**: "其他设置" → "性能优化参数"

| 配置项 | 类型 | 默认值 | 范围 | 说明 |
|--------|------|--------|------|------|
| 设置页面分析线程数 | 数字 | 3 | 1-10 | 设置页面股票分析使用的线程数 |
| 设置页面启用多线程 | 开关 | 开启 | - | 是否启用多线程并行分析 |
| 主页未收盘预测最大线程数 | 数字 | 5 | 1-10 | 主页未收盘预测的最大线程数（用于限制动态线程数） |

---

### 3.5 缓存参数配置

**位置**: "其他设置" → "缓存参数"

| 配置项 | 类型 | 默认值 | 范围 | 说明 |
|--------|------|--------|------|------|
| 预测结果最小缓存时长 | 数字 | 5 | 1-60 | 预测结果的最小缓存时长（分钟） |
| 预测结果最大缓存时长 | 数字 | 15 | 1-60 | 预测结果的最大缓存时长（分钟） |
| 实时数据缓存时长 | 数字 | 10 | 1-60 | 实时数据的缓存时长（秒） |

---

## 四、实施建议

### 4.1 优先级

**🔴 高优先级**:
1. ML模型动态权重配置（影响预测准确性）
2. X2指标参数配置（影响技术分析准确性）
3. 异常检测参数配置（影响预测可靠性）

**🟡 中优先级**:
4. 多线程配置（影响性能，但已有默认值）
5. 实时策略缓存参数（影响性能，但已有默认值）

---

### 4.2 实施步骤

1. **扩展配置表结构**:
   - 在 `prediction_config` 表中添加新的配置项
   - 更新 `PredictionConfigManager._get_param_definitions()` 方法

2. **更新前端界面**:
   - 在 `settings.html` 中添加新的配置区域
   - 添加相应的表单控件和验证逻辑

3. **更新后端逻辑**:
   - 修改硬编码的参数，改为从配置读取
   - 确保配置变更后立即生效（热更新）

4. **添加配置说明**:
   - 为每个新配置项添加工具提示
   - 提供默认值和推荐值说明

---

## 五、配置项分类建议

### 5.1 配置区域划分

```
参数设置
├── 预测因子权重配置
│   ├── 核心因素（新闻、资金流向、市场情绪）
│   ├── 技术分析（技术指标、板块轮动）
│   ├── 辅助因素（历史模式、美股板块、估值指标）
│   └── ML模型权重配置（新增）⭐
│
├── 技术指标参数配置
│   ├── MA均线参数
│   ├── RSI参数
│   ├── MACD参数
│   ├── KDJ参数
│   ├── CCI参数
│   └── X2指标参数（新增）⭐
│
└── 其他设置
    ├── 基础设置（最小置信度、回看天数）
    ├── 异常检测参数（新增）⭐
    ├── 性能优化参数（新增）⭐
    └── 缓存参数（新增）⭐
```

---

## 六、总结

### 6.1 当前状态

✅ **已有功能**: 预测因子权重、技术指标参数、基础设置  
❌ **缺少配置**: ML模型动态权重、X2指标参数、异常检测参数、多线程配置、缓存参数

### 6.2 建议

1. **立即添加**: ML模型动态权重配置、X2指标参数配置、异常检测参数配置
2. **后续添加**: 多线程配置、缓存参数配置（已有默认值，优先级较低）

### 6.3 预期效果

- ✅ 提高系统可配置性
- ✅ 方便用户根据实际情况调整参数
- ✅ 支持参数调优实验
- ✅ 提升用户体验

---

**报告生成时间**: 2026-01-16  
**状态**: 待实施
