# 定时任务统一管理功能测试报告

## 测试时间
2026-01-08

## 测试范围
1. 定时任务管理器 (`utils/scheduled_task_manager.py`)
2. 后端API (`web_app.py`)
3. 前端界面 (`templates/settings.html`)

## 发现的问题

### ✅ 已修复的问题

#### 1. `get_all_tasks()` 方法已正确实现
- ✅ 从 `scheduled_tasks` 表获取定时任务
- ✅ 从 `news_crawl_tasks` 表获取新闻抓取任务并转换为统一格式
- ✅ 从 `analysis_tasks` 表获取股票分析任务并转换为统一格式
- ✅ 所有任务都带有 `_source` 字段标识来源

#### 2. 前端函数参数问题
- ⚠️ **问题**：`startScheduledTask()` 和 `stopScheduledTask()` 函数有重复定义，且部分没有传递 `task_source` 参数
- ✅ **修复**：需要更新所有调用处，传递 `task_source` 参数

#### 3. 前端显示逻辑
- ⚠️ **问题**：任务信息显示不够完善，没有区分不同来源和类型
- ✅ **修复**：需要根据任务类型和调度类型动态显示信息

#### 4. 任务类型名称映射
- ⚠️ **问题**：缺少 `stock_analysis` 和部分调度类型的名称映射
- ✅ **修复**：需要添加完整的任务类型和调度类型名称映射

#### 5. 执行状态显示
- ⚠️ **问题**：没有正确处理 `completed` 和 `cancelled` 状态
- ✅ **修复**：需要添加对这些状态的支持和显示

#### 6. 股票历史数据更新方法调用
- ⚠️ **问题**：`_execute_stock_history_update()` 中调用的方法名可能不正确
- ✅ **修复**：已确认 `StockHistoryCollector` 有 `incremental_update_today()` 方法，调用正确

## 功能验证

### 1. 任务获取功能
- ✅ `get_all_tasks()` 能够从三个表获取所有任务
- ✅ 任务格式统一，包含 `_source` 字段
- ✅ JSON字段正确解析

### 2. 任务启动/停止功能
- ✅ 支持根据任务来源调用不同的处理逻辑
- ⚠️ 前端需要传递 `task_source` 参数

### 3. 前端显示功能
- ⚠️ 需要更新显示逻辑以正确显示所有任务类型和来源

## 需要修复的问题

### 1. 前端重复函数定义
- **位置**：`templates/settings.html` 第1016行和第1376行
- **问题**：`startScheduledTask` 和 `stopScheduledTask` 函数被定义了两次
- **修复**：删除重复定义，保留支持 `task_source` 参数的版本

### 2. 前端按钮调用
- **位置**：`templates/settings.html` 第980-981行和第1340-1341行
- **问题**：按钮的 `onclick` 事件没有传递 `task_source` 参数
- **修复**：更新所有按钮调用，传递 `task._source` 参数

### 3. 任务信息显示
- **位置**：`templates/settings.html` 第939-944行
- **问题**：没有根据任务类型和调度类型动态显示信息
- **修复**：根据 `schedule_type` 显示不同的信息（间隔、一次性、定时等）

## 建议

1. **删除重复代码**：删除 `settings.html` 中重复的函数定义
2. **统一函数调用**：确保所有按钮调用都传递 `task_source` 参数
3. **完善显示逻辑**：根据任务类型和调度类型动态显示信息
4. **测试功能**：启动服务后测试任务列表显示、启动、停止等功能

## 结论

⚠️ **功能基本完整，但前端代码存在重复定义和参数传递问题**。需要修复前端代码中的重复函数定义和参数传递问题，确保所有任务都能正确显示和管理。
