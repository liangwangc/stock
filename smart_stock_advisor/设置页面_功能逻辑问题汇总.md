## 设置页面功能与逻辑问题汇总（阶段性草稿）

> 说明：本文件只记录目前已经详细审查过的模块和问题，后续审查其他模块时会继续往下追加。当前覆盖模块：  
> - 模型学习相关（性能评估 / 回测 / 参数优化）  
> - 参数设置（panel-params）  
> - 备份管理（panel-backup）  
> - 模型训练（panel-model-training）  
> - 股票分析任务管理（panel-task）  
> - 新闻抓取（panel-news）  
> - 股票/其他数据获取（panel-stock-data）  
> - 定时任务管理（panel-scheduled-tasks）  
> - 用户管理（panel-users）  
> - 模型学习二轮深审（性能评估/参数优化/在线学习/自适应/定时任务 — 2026-02-12）
> - 回测引擎 + 参数优化器 三轮深审（交易逻辑统一/风控价格/贝叶斯验证bug — 2026-02-12）

---

### 一、模型学习相关功能

对应位置：
- 后端：`web_app.py` 中 `/api/model/evaluate`、`/api/backtest/run`、`/api/model/optimize` 等路由  
- 后端：`utils/model_performance_evaluator.py`、`utils/backtest_engine.py`、`utils/model_optimizer.py`  
- 前端：`templates/settings.html` 中 `panel-model-learning` 区域

#### 1. 性能评估（点击后"没结果"的问题）

- **问题 1-1：API 成功 / 失败语义不清**
  - 现象：当 **预测评估**和**交易评估**都没数据或失败时，`/api/model/evaluate` 依然返回 `success: True`，只是 `data` 里字段为空或缺失。前端拿到 `success: True` 会直接尝试渲染，而不是弹出错误提示，用户体验就是"点了没反应"。
  - 影响：  
    - 用户无法区分"评估真的没问题"与"实际上根本没有可评估的数据"。  
    - 前端逻辑会继续访问缺失字段，进一步引出 NaN 等显示问题。

- **问题 1-2：前端对缺失字段缺乏容错**
  - 现象：`displayPerformanceResults` 中直接使用 `data.direction_accuracy * 100`、`data.win_rate * 100` 等表达式，若字段不存在或为 `undefined`，结果为 `NaN`，最终展示为 "NaN%"。  
  - 影响：  
    - 当后端没有返回某些字段时，页面指标卡、图表会出现 NaN，肉眼不一定马上发现，容易误解为系统"没更新数据"。

> 现状：上述问题在最近一次调整中已经修复，但这里仍保留为历史问题记录，方便后续回顾设计。

#### 2. 回测功能（"效果很差"的问题）

- **问题 2-1：早期版本中，买入成本字段混淆，导致收益率严重失真**
  - 早期逻辑（问题版本）：  
    - 买入时将"总成本"直接写入 `cost_price` 字段；  
    - 卖出时用"每股价格"与 `cost_price` 比较来计算收益率。  
    - 示例：买入总成本约 30000 元，`actual_price` 约 25.5 元，公式变成 `(25.5 - 30000)/30000`，得到约 -99.9% 的"巨亏"，几乎所有交易都会被算成极端亏损。
  - 影响：  
    - 回测曲线高度悲观，夏普比率、最大回撤等所有基于收益率的指标都严重失真。  
    - 用户直观感受就是"回测一塌糊涂"，对模型完全失去信心。

- **问题 2-2：持仓估值逻辑过于简化**
  - 早期 `_calculate_portfolio_value` 使用 **成本价** 估算持仓市值，而不是实际市场价格。  
  - 影响：  
    - 最大回撤、日度收益序列、夏普比率等全部偏离真实情况。  
    - 即使单笔交易利润逻辑正确，组合层面的风险收益表现仍然严重走样。

- **问题 2-3：前端再次放大利润百分比（双重 *100）**
  - 后端回测指标（尤其是 `metrics.total_return`、`metrics.win_rate`、`metrics.max_drawdown` 等）本身已经是百分比数值（5.23 表示 5.23%）。  
  - 前端 `renderBacktestResults` 中又把这些值乘以 100 再显示，导致视觉上变成 523.00%。  
  - 影响：  
    - 用户会误以为策略极端波动（要么爆亏要么爆赚），进一步削弱对回测结果的信任。

> 当前版本：成本价/估值/百分比重复缩放等问题已经修复；这里记录为设计缺陷历史。

#### 3. 参数 / 权重优化

- **问题 3-1（设计层面致命）：优化器搜索的参数在回测引擎中根本没用上**
  - 早期版本设计：  
    - `ModelOptimizer` 默认搜索的是一组"权重参数"，如：`news_weight`、`capital_flow_weight`、`market_weight`、`technical_weight`、`history_weight` 等。  
    - 但 `BacktestEngine.backtest_with_parameters` 实际只读取以下**交易参数**：  
      - `buy_threshold`（买入阈值）  
      - `sell_threshold`（卖出阈值）  
      - `min_confidence`（最小置信度）  
      - `stop_loss_pct`（止损）、`take_profit_pct`（止盈）  
      - `max_position_pct`（仓位比例）  
    - 权重相关字段在回测引擎中完全没有被读取或使用。
  - 直接后果：  
    - 无论 `ModelOptimizer` 如何调整权重组合，回测引擎都在用"同一套交易参数"做回测，结果曲线几乎一样。  
    - "最优权重组合"实际上只是从一堆结果极其相似的回测里，随机挑了一个分数略高的，优化过程名存实亡。

- **问题 3-2：优化评分函数量纲不一致，夏普比率被极度放大**
  - 早期 `_calculate_optimization_score` 的结构大致为：  
    \[
    \text{score} = total\_return * 0.4 + sharpe\_ratio * 0.3 + (1 - max\_drawdown) * 0.2 + win\_rate * 0.1
    \]
  - 问题在于：  
    - `total_return`、`win_rate`、`max_drawdown` 等都在 0~1 的小数级别；  
    - `sharpe_ratio` 通常在 [-1, 3] 区间。  
    - 在没有归一化的情况下，夏普比率对得分的影响远大于其他指标，导致优化器几乎只追求"看起来夏普比率高"的解。
  - 实际效果：  
    - 优化出的参数组合在风险/收益/胜率上的表现不一定更好，只是某些样本上的夏普被抬高。  
    - 用户从实际回测曲线体感不到"明显变好"，会觉得"优化没啥用"。

---

### 二、参数设置模块（panel-params）

对应位置：
- 后端：`utils/prediction_config_manager.py`  
- 前端：`templates/settings.html` 中 `panel-params` 区域及相关 JS 函数

#### 1. 权重校验标准前后端矛盾（致命）

- 后端验证逻辑：  
  - `validate_config` 要求 5 个传统因子权重（`news_weight`、`capital_flow_weight`、`market_weight`、`technical_weight`、`history_weight`）之和 **必须为 1.0（±0.01 容差）**。
- 前端引导文案 & 校验逻辑：  
  - 文案提示："传统因子权重之和约为 0.65，另 0.35 由 ML 模型贡献"。  
  - 前端用 0.65 作为"绿色通过"的判断标准。  
- 结果：  
  - 用户按照前端文案设置出的"合理权重"在后端校验总是失败。  
  - 即使前端显示"权重合计 0.65 ✔"，后端依然报"权重总和应为 1.0"，**导致配置永远无法通过后端校验并激活**。

#### 2. `performance` 分类参数读取时被静默丢弃（严重）

- 后端 `_get_param_definitions` 中定义了 `performance` 分类的一系列参数（如历史任务最大并发、预测缓存最小持续时间等）。  
- `_save_config_values` 能将这些参数正确写入 `prediction_config_values`。  
- 但 `get_config` 初始化时只创建了：`prediction`、`indicator`、`news`、`trading` 四个 key，没有 `performance`。  
- 后续从数据库读出 `category = 'performance'` 的记录时，由于 `category not in config_dict`，被直接忽略。  
- 影响：  
  - 相关参数可以被保存到数据库，但在读取到内存配置结构时全部丢失。  
  - 前端如果未来添加 UI 来编辑性能相关参数，也会出现"保存后读不回来"的现象。

#### 3. X2 指标参数保存路径缺失（严重）

- 前端 `loadConfigValues` 和默认配置加载中都有 X2 相关字段（阈值与得分共计 8 个）。  
- 但 `getCurrentConfigValues` 收集要提交的配置时，只列出了传统技术指标参数（MA/RSI/MACD/KDJ/CCI），完全遗漏 X2 系列参数。  
- 实际效果：  
  - X2 参数可以展示、可以被用户修改，但点击"保存配置"后，这些参数不会出现在提交 payload 中。  
  - 下一次重新打开设置页面时，X2 相关字段恢复为默认值或旧值，用户以为"系统没记住我刚才的设置"。

#### 4. 部分交易参数前端有，后端无定义（严重）

- 前端在 `getCurrentConfigValues` 中收集了若干交易相关参数，如：  
  - `max_total_position_pct`（组合总仓位上限）  
  - `max_correlation`（持仓相关性上限）  
  - `enable_risk_control`（是否启用风险控制）  
- 但在后端 `_get_param_definitions` 的 `trading` 分类中，没有对应 key 的定义。  
- 结果：  
  - `_save_config_values` 会遍历所有提交参数，只对已在 `param_definitions` 中登记的键执行 INSERT。  
  - 这几个额外的交易参数被直接 `continue` 掉，相当于用户做的修改从未真正进入数据库。

#### 5. `news` 分类前端完全缺失（严重）

- 后端定义了 `news` 分类若干配置（新闻条数、情绪阈值、是否包含行业新闻等）。  
- 前端目前没有任何针对 `news` 分类的 UI：  
  - 不展示现有值；  
  - 不允许用户修改；  
  - 也不会把 `news` 分类写回到提交值里。  
- `_save_config_values` 的策略是：  
  - 先 `DELETE FROM prediction_config_values WHERE config_id = ?`；  
  - 再插入新的分类参数。  
- 这意味着：  
  - 任何一次"只改 prediction / indicator / trading"的配置保存操作，都会把原本存在的 `news` 分类参数删掉，而且不会再写回新值，**形成"操作一次别的配置，顺带清空所有 news 配置"的副作用**。

#### 6. 配置删除历史记录被级联删除（严重）

- 删除配置流程：  
  - 先通过 `get_config` 读取当前配置值，调用 `_record_history(config_id, 'delete', old_values, None, ...)` 记录一条删除历史；  
  - 然后执行 `DELETE FROM prediction_config WHERE id = ?` 真正删除配置主记录。  
- 问题在于：  
  - `prediction_config_history` 表在建表时通过 `FOREIGN KEY (config_id) REFERENCES prediction_config(id) ON DELETE CASCADE` 与主表关联。  
  - 当删除主表记录时，会将所有引用该 `config_id` 的历史记录一并删掉。  
- 结果：  
  - 删除配置之前刚写入的 `'delete'` 操作历史马上就被级联删除了，数据库中最终没有留下任何与删除行为相关的审计记录。

#### 7. 其他设计与实现层面问题（概述）

（以下问题严重程度相对较低，这里只做摘要，详细可在后续精简为修复任务清单）

- `clear_cache(config_id=None)` 使用不可重入锁并在持锁期间调用 `get_config`，存在潜在死锁风险。  
- JS 端多处用 `value || default` 来提供默认值，导致合法的 `0` 被错误替换为默认值。  
- `_save_config_values` 的"先删后插"操作不包裹在显式事务中，异常情况下有中途失败导致部分分类参数永久丢失的风险。

---

### 三、备份管理模块（panel-backup）

对应位置：
- 后端：`utils/backup_manager.py`、`web_app.py` 中 `/api/backup/*` 路由  
- 前端：`templates/settings.html` 中 `panel-backup` 区域及相关 JS

#### 1. 扫描出的备份无法恢复 / 删除（严重）

- 当 metadata 中没有某条备份记录时，`list_backups` 会调用 `_scan_backup_files` 直接扫描备份目录并返回文件列表，用于前端展示。  
- 但 `restore_backup` / `delete_backup` 在查找目标时只遍历 `self.metadata['backups']`，扫描新增的备份文件没有对应的 metadata entry，因此会提示"备份 ID 不存在"，无法真正执行恢复或删除。  
- 实际效果：  
  - UI 上可以看到备份文件，但按钮点了也不生效，让人感觉"备份功能不可靠"。

#### 2. 备份 ID 生成方式容易重复（严重）

- 新备份 ID 使用 `len(self.metadata.get('backups', [])) + 1` 生成。  
- 当中间删除过某些备份（例如已有 ID:1,3），再次创建备份会得到 ID=3，与现有记录冲突。  
- 后续操作（恢复 / 删除）若以 ID 为唯一识别键，容易误操作到另一份备份。

#### 3. 并发与文件名冲突问题（中）

- 备份文件名使用时间戳 `%Y%m%d_%H%M%S`，精确到秒：同一秒内发起多次备份会覆盖同名文件。  
- 元数据读写未加锁、防并发机制，多个进程或多实例同时进行备份、删除、恢复操作时，存在竞态条件。

#### 4. 安全与体验方面的小问题（概述）

- 删除 / 恢复对话框仅用一次 `confirm` 对话框做防误操作，未提供例如"需输入完整备份名确认"等更严格防护。  
- 在前端渲染备份列表时，部分字符串（如文件名、描述）直接插入 HTML / JS 字符串模板，理论上存在 XSS 隐患（前提是攻击者能控制文件名或 metadata 内容）。

---

### 四、模型训练模块（panel-model-training）

对应位置：
- 后端：`web_app.py` 中 `/api/ml/train/*`、`/api/ml/models/*` 路由  
- 后端：`utils/ml_model_trainer.py`  
- 前端：`templates/settings.html` 中 `panel-model-training` 及对应 JS 函数

#### 1. 训练任务并发控制不足（高）

- 后端在启动基础训练 (`/api/ml/train/base`) 或微调训练 (`/api/ml/train/finetune`) 时，没有严格检查当前是否已经存在**同类型正在运行的任务**。  
- 前端按钮点击也未在调用前先查询当前状态并做"已有任务在运行"的拦截。  
- 结果：用户可在短时间内多次点击，意外启动多个训练任务，造成：  
  - 数据库和文件 IO 冲突；  
  - 训练日志和任务状态表 `ml_training_tasks` 内容混乱；  
  - 资源占用激增。

#### 2. 训练任务表只增不减（高）

- `ml_training_tasks` 在任务启动时 append 一条记录，任务完成或失败后并未有统一逻辑清理旧记录。  
- 服务长期运行后，内存中会保存大量历史任务信息，占用内存、影响查询效率。

#### 3. 进度反馈不真实（中）

- 训练进度在后端仅在任务开始时设置为约 5%，任务结束时置为 100%。  
- 中间执行过程不会持续更新进度。  
- 前端轮询拿到的进度长期停在 5%，直到任务结束突然跳到 100%，用户主观体验就是"进度条卡住不动，很久才一下子完成"，容易误以为应用卡死。

#### 4. 多任务状态获取逻辑不清晰（中）

- `/api/ml/train/status` 在存在多个任务记录时，仅返回第一个 `running_tasks[0]` 作为当前状态。  
- 如果基础训练和微调训练重叠，或用户连续多次操作，状态信息可能与用户当前关注的任务不一致。

---

### 五、股票分析任务管理模块（panel-task）

对应位置：
- 后端：`web_app.py` 中 `/api/tasks/*` 相关路由  
- 可能相关后端模块：`utils/task_history_db.py` 等任务记录工具  
- 前端：`templates/settings.html` 中 `panel-task` 区域及 `startAnalysis`、`updateTaskStatus`、`stopAnalysis`、`refreshTaskHistory` 等 JS 函数

#### 1. 任务启动阶段的问题

- **问题 T1：task_id 可能在同一秒内重复（高）**  
  - 生成规则为 `task_{YYYYMMDD_HHMMSS}`，仅精确到秒。  
  - 若同一秒内用户多次点击"开始分析"，会生成相同的 `task_id`。  
  - 后果：  
    - 后端 `running_tasks` 字典中后启动的任务会覆盖先前的记录；  
    - 数据库中以 `task_id` 为主键或唯一键的地方也可能产生覆盖，导致历史记录混乱。

- **问题 T2：任务启动存在竞态条件（高）**  
  - 当前流程是：先 `thread.start()` 启动分析线程，再在 `task_lock` 下把任务信息记录进 `running_tasks`。  
  - 如果线程启动后很快执行到"检查自己是否在 running_tasks 中"的逻辑，可能发现自身未注册，从而直接结束或执行异常路径。  
  - 后果：  
    - 前端收到"任务已启动"的响应，但实际后台线程很快退出或根本没正确登记，进度长时间停留在 0/初始值，看起来像"假启动"或"卡住不动"。

- **问题 T3：后端对同一用户的并发任务无硬限制（中）**  
  - `api_start_task` 未检查当前用户是否已有运行中的分析任务。  
  - 前端只在当前标签页内通过 `currentTaskId` 和按钮禁用来防重复点击，在多标签页或多客户端下无法限制。  
  - 后果：  
    - 同一用户可以在多标签页同时启动多份重型分析任务，造成数据库和 CPU 资源的大量竞争。

#### 2. 任务状态轮询与进度更新

- **问题 T4：状态轮询不依赖当前面板（中）**  
  - JS 使用 `setInterval`，只要 `currentTaskId` 非空就每 2 秒轮询 `/api/tasks/{task_id}`。  
  - 不判断"当前是否仍停留在股票分析任务管理面板"。  
  - 后果：  
    - 用户切换到其他设置面板后，轮询依然持续，对后端造成不必要的负载。

- **问题 T5：轮询没有超时或最大次数控制（低）**  
  - 清理轮询的唯一逻辑是 `currentTaskId` 被置空。  
  - 若异常情况下 `currentTaskId` 未被正确清空，轮询会一直运行下去。  

- **问题 T6：进度更新存在一定滞后但可接受（低）**  
  - 后端为减少消息量，常常每处理若干股票才 emit 一次进度事件；  
  - 在股票数量较多时，前端看到的进度会以"阶梯"形式前进，不是平滑曲线。  

#### 3. 任务停止与权限控制

- **问题 T7：停止任务缺少用户级权限校验（高）**  
  - `api_stop_task` 仅校验用户是否已登录，并未校验该 `task_id` 是否属于当前用户（例如 `task.user_id == session['user_id']`）。  
  - 后果：  
    - 任何登录用户只要知道一个合法 `task_id`，就可以停止他人的任务，属于典型的越权操作风险。

- **问题 T8：停止标记与任务记录不清理（高）**  
  - 任务结束（成功 / 失败 / 被停止）后，`running_tasks` 和 `task_stop_flags` 中对应条目没有统一的清理逻辑。  
  - 后果：  
    - 服务长期运行后，这两个结构会不断膨胀，造成内存浪费；  
    - 某些内部逻辑以"是否存在于 running_tasks / task_stop_flags"来判断当前状态时，可能被过期记录误导。

- **问题 T9：多次停止同一任务的语义不清晰（低）**  
  - 再次调用停止接口时，后端返回"无法停止，任务状态不是 running"，但不会造成更严重的问题。  

#### 4. 历史记录与可见性

- **问题 T10：历史记录未按用户隔离（高）**  
  - `/api/tasks/history` 在查询历史时，只按时间排序和 limit 限制，SQL 中没有 `WHERE user_id = ?` 过滤条件。  
  - 后果：  
    - 任意登录用户可以看到所有用户的任务历史（包括其他人的分析范围、股票池、时间等信息），存在数据隐私和权限边界问题。

- **问题 T11：前端历史列表列内容与表头含义不匹配（中）**  
  - "分析类型""分析内容"等表头对应的列实际填入的是 `limitText`（数量）、`sortText`（排序方式），而不是抽象后的"按数量 / 按股票""分析维度"等。  
  - 影响：  
    - 用户阅读历史记录时难以快速理解每一列的含义，降低可用性。

- **问题 T12：历史记录没有分页（中）**  
  - 前端固定请求 `limit=50`，没有 offset / page 参数；后端也只实现了简单的 limit。  
  - 后果：  
    - 只保留最近 50 条记录的可见性，更早的历史分析任务对用户来说等同于"丢失"，不利于长期使用和审计。

#### 5. 并发与多客户端场景

- **问题 T13：多标签页 / 多客户端下的状态广播与过滤（中）**  
  - 后端通过 WebSocket 广播任务事件（task_started / task_completed / task_cancelled）给所有连接。  
  - 前端依赖 `task_id === currentTaskId` 来过滤"当前关心的任务"。  
  - 在多标签页或多用户场景下，可能存在某个标签页接收到与自身无关的任务事件、短暂显示不一致状态的情况。

---

### 六、新闻抓取模块（panel-news）

对应位置：
- 后端：`web_app.py` 中 `/api/news/tasks*`、`/api/news/llm/*` 路由  
- 后端：`utils/news_task_manager.py` 及新闻抓取相关模块  
- 前端：`templates/settings.html` 中 `panel-news` 区域及任务管理 / LLM 分析相关 JS 函数

#### 1. 任务管理（增删改查）中的关键问题

- **问题 N1：删除任务只删旧表，不删统一调度主表（致命）**  
  - `delete_task()` 仅删除 `news_crawl_tasks`，未同步删除 `scheduled_tasks` 中对应记录。  
  - 后果：  
    - 用户删除后列表里任务可能仍出现；  
    - 调度器仍可能继续执行"已删除"任务，形成强一致性问题。

- **问题 N2：更新任务只更新旧表，不更新统一调度主表（致命）**  
  - `update_task()` 仅更新 `news_crawl_tasks`，而查询展示通常优先读 `scheduled_tasks`。  
  - 后果：  
    - 用户编辑任务后，界面和实际调度仍按旧配置执行；  
    - 用户会感知为"修改不生效"。

- **问题 N3：删除前未先停止调度器中的任务（中）**  
  - 删除逻辑未显式调用统一调度器的 stop/unregister 流程。  
  - 后果：  
    - 在某些状态下任务删除后仍可能被调度线程触发。

#### 2. 调度与参数逻辑问题

- **问题 N4：`hourly` 模式忽略 `interval_minutes`（高）**  
  - 创建任务时允许填写分钟间隔（例如 90 分钟），但 `hourly` 调度分支实际上按"每 1 小时"固定执行。  
  - 后果：  
    - 用户配置"每 1.5 小时"会被静默改成"每 1 小时"，执行频率与预期不符。

- **问题 N5：`interval_minutes` 小数值健壮性不足（中）**  
  - 调度库对整数间隔更稳定；当前流程允许小数输入，边界场景下可能调度异常。  
  - 后果：  
    - 间隔可能被隐式取整、报错或出现不可预测行为。

#### 3. LLM 分析链路问题

- **问题 N6：LLM 分析启动存在竞态，可重复启动（高）**  
  - 启动接口中，状态从检查到置为 running 之间存在窗口期，第二个请求可能穿透检查。  
  - 后果：  
    - 同时运行多个 LLM 分析进程，出现资源竞争与结果覆盖。

- **问题 N7：子进程模式进度不可实时更新（高）**  
  - 回退到 `subprocess` 执行时，日志与进度是"结束后一次性读取"，中间无法细粒度更新。  
  - 后果：  
    - 前端进度条长期停在 0 或低值，用户误判为"卡住"。

- **问题 N8：进度回退算法可能引用全局统计导致失真（中）**  
  - 在部分状态下用全局"已分析/未分析数量"推断本次任务进度，范围不一定匹配当前任务。  
  - 后果：  
    - 进度可能跳变、提前 100% 或长期不动。

- **问题 N9：前端轮询异常时未稳妥停止（中）**  
  - 轮询链路在异常分支下仍可能继续触发下一次检查。  
  - 后果：  
    - 失败后仍持续请求状态接口，增加后端负担。

#### 4. 权限与多用户隔离

- **问题 N10：任务操作与 LLM 分析缺少细粒度权限隔离（中）**  
  - 当前主要做"是否登录"校验，未细分管理员/普通用户能力，也缺少明确的多租户隔离。  
  - 后果：  
    - 多用户环境下容易出现误操作或越权风险。

#### 5. 前端交互与一致性问题（次要）

- **问题 N11：编辑接口未传 `sources`，后端却允许更新该字段（中）**  
  - 前后端字段设计不一致，后续扩展容易踩坑。  

- **问题 N12：关闭编辑弹窗使用宽泛选择器（低）**  
  - 可能误关闭其他固定定位弹窗，造成交互异常。

---

### 七、股票 / 其他数据获取模块（panel-stock-data）

对应位置：
- 后端：`web_app.py` 中 `/api/stock-data/tasks*`、`/api/other-data/tasks*`、`/api/scheduled/tasks*`（source=scheduled_tasks）  
- 后端：`utils/scheduled_task_manager.py`（任务历史、调度执行）  
- 前端：`templates/settings.html` 中 `panel-stock-data` 区域及相关 JS 函数

#### 1. 任务创建与调度参数问题

- **问题 SD1：月度调度参数链路不完整（高）**  
  - 股票数据任务支持"monthly"类型，但前端创建表单未提供 `schedule_month_day` 输入，后端创建接口也未完整消费该参数。  
  - 后果：  
    - 用户无法配置"每月几号执行"，任务会退化为固定默认日期执行（通常是每月1号），与预期不符。

- **问题 SD2：其他数据任务对必需参数校验不充分（中）**  
  - 某些任务类型（如融资融券、主力资金、行业信息）理论上需要明确股票列表或相关参数，但创建接口未强制校验。  
  - 后果：  
    - 任务可成功创建，但运行时才暴露缺参失败，造成"创建成功但执行报错"的延迟故障。

#### 2. 历史记录链路问题（影响可用性）

- **问题 SD3：历史接口 `limit=None` 会触发 SQL 风险（致命）**  
  - 任务历史 API 在某些调用路径下将 `limit=None` 传给底层 `get_task_history()`；SQL 使用 `LIMIT %s` 时若传入 None 可能导致异常。  
  - 后果：  
    - 历史记录接口返回失败或空数据，页面出现"有任务但看不到历史"。

- **问题 SD4：前端历史数据是"截断式拉取"（高）**  
  - 前端先请求最多 1000 条任务，再对每个任务最多拉 100 条历史，本地再合并分页。  
  - 后果：  
    - 当任务量/历史量较大时，实际展示是"被截断后的子集"，不是完整历史；用户误以为数据丢失。

- **问题 SD5：历史耗时字段映射错误（高）**  
  - 前端显示使用 `item.duration`，而后端字段通常为 `duration_seconds`（或其他命名），映射缺失。  
  - 后果：  
    - UI 显示 `undefined秒` 或空值，影响历史记录可读性。

#### 3. 前后端类型映射与展示问题

- **问题 SD6：其他数据任务类型前缀不一致（致命）**  
  - 前端名称映射按 `other_data_collection_*`，后端真实类型是 `other_data_*`。  
  - 后果：  
    - 任务列表中"其他数据任务"无法正确翻译为中文名称，直接显示技术名，影响理解与运维判断。

#### 4. 交互与健壮性问题（次要）

- **问题 SD7：任务启动/停止按钮缺少防重复点击（低）**  
  - 短时间多次点击可能触发重复请求。  

- **问题 SD8：stock-data 任务列表刷新无显式 loading（低）**  
  - 请求慢时用户容易误判为页面卡住。  

- **问题 SD9：编辑/创建弹窗关闭选择器过于宽泛（中）**  
  - 使用 `querySelector('div[style*=\"position: fixed\"]')`，可能误关闭其他弹窗。

#### 5. 性能层面问题

- **问题 SD10：后端先全量取任务再内存过滤（低）**  
  - `/api/stock-data/tasks` 先取全部任务，再在 Python 端筛选 stock/other_data 类型。  
  - 后果：  
    - 任务量大时响应变慢，CPU与内存浪费。

---

---

### 八、定时任务管理模块（panel-scheduled-tasks）

对应位置：
- 后端：`web_app.py` 中 `/api/scheduled/tasks*`、`/api/scheduled/scheduler/status` 等路由  
- 后端：`utils/scheduled_task_manager.py`  
- 前端：`templates/settings.html` 中 `panel-scheduled-tasks` 区域及 `refreshScheduledTasks`、`startScheduledTask`、`stopScheduledTask`、`deleteScheduledTask`、`editScheduledTask` 等 JS 函数

#### 1. 多来源任务操作未传递 source 参数（致命）

- **问题 ST1：前端启动/停止/删除/编辑均不传 task_source**
  - `get_all_tasks` 返回混合任务：来自 `scheduled_tasks`、`news_crawl_tasks`、`analysis_tasks` 三张表，每个任务带有 `_source` 字段标识来源。
  - 但 `startScheduledTask`、`stopScheduledTask`、`deleteScheduledTask`、`editScheduledTask` 调用 API 时均未传递 `source` 参数。
  - 后端 `delete_task` 在 `task_source=None` 时：先 `get_task(task_id)`（只查 `scheduled_tasks`），对 news/analysis 任务返回 None，于是 `task_source` 默认 `'scheduled_tasks'`，执行 `DELETE FROM scheduled_tasks WHERE id = ?`。
  - 后果：  
    - 删除新闻任务时，可能误删 `scheduled_tasks` 中同 ID 的其他任务，或删除失败（ID 不存在）；  
    - 编辑/获取新闻任务时，`get_task` 查不到，返回"任务不存在"；  
    - 启动/停止新闻任务时，会错误地按 `scheduled_tasks` 处理。

- **问题 ST2：编辑弹窗仅适配 daily，不支持 interval/monthly**
  - 编辑表单固定为 `schedule_type: 'daily'`，只有时间、星期，无 `schedule_month_day`、`interval_minutes`。  
  - 对 `interval` 或 `monthly` 任务编辑保存后，调度类型相关配置被覆盖或丢失。

#### 2. 调度器与任务来源逻辑问题

- **问题 ST3：get_task 无法获取 news_crawl_tasks / analysis_tasks 任务**
  - `get_task(task_id)` 只执行 `SELECT * FROM scheduled_tasks WHERE id = %s`，不查 `news_crawl_tasks`、`analysis_tasks`。  
  - 编辑、更新、历史查询依赖 `get_task`，对新闻抓取、股票分析任务一律返回"不存在"。

- **问题 ST4：执行历史表仅关联 scheduled_tasks**
  - `get_task_history` 查询 `scheduled_task_history`，其 `task_id` 对应 `scheduled_tasks.id`。  
  - `news_crawl_tasks` 任务的历史可能存于其他表或结构，当前接口无法正确返回。

- **问题 ST5：analysis_tasks 删除与停止语义不清晰**
  - `delete_task` 对 `analysis_tasks` 直接返回 False，提示"股票分析任务不支持删除"。  
  - 列表仍展示 analysis_tasks，用户可点击删除/停止，但操作无实际效果，容易造成困惑。

#### 3. 创建任务与月度调度

- **问题 ST6：创建弹窗缺少 schedule_month_day 与 interval 配置**
  - 创建定时任务弹窗未提供 `schedule_type` 选择（daily/weekly/monthly/interval），也未提供 `schedule_month_day`、`interval_minutes` 输入。  
  - 后端 `create_task` 已支持 `schedule_month_day`，但前端无法配置，月度任务只能使用默认 1 号。

- **问题 ST7：创建时 task_config 与 schedule_type 可能不一致**
  - 选择"新闻抓取"等类型时，应使用 `interval`，但创建表单仍以 `daily` 时间为主，可能导致调度类型与业务不符。

#### 4. 前端交互与健壮性

- **问题 ST8：关闭弹窗使用宽泛选择器（中）**
  - `updateScheduledTask` 成功后用 `querySelectorAll('div[style*="position: fixed"]')` 关闭所有固定定位弹窗，可能误关其他无关弹窗。

- **问题 ST9：启动/停止/删除无防重复点击（低）**
  - 无 loading 或 disabled 状态，快速多次点击可能发起重复请求。

- **问题 ST10：任务类型名称映射遗漏 other_data_ 前缀（中）**
  - `getTaskTypeName` 中 `other_data_collection_*` 有映射，但实际任务类型可能是 `other_data_*`（无 `collection`），会导致显示为原始类型名，与股票数据模块问题 SD6 类似。

#### 5. 与新闻/股票数据模块的联动

- **问题 ST11：与 panel-news、panel-stock-data 的交叉操作**
  - 同一新闻任务在 panel-news 与 panel-scheduled-tasks 均可展示，若两处实现不一致（如 source 传递、删除逻辑），会出现"在此删得掉、在彼删不掉"等现象。  
  - 需统一 source 传递及后端对多来源任务的处理逻辑。

---

### 九、用户管理模块（panel-users）

对应位置：
- 后端：`web_app.py` 中 `/api/users`、`/api/users/<id>`、`/api/users/login-history` 路由  
- 后端：`utils/user_manager.py`  
- 前端：`templates/settings.html` 中 `panel-users` 区域及 `refreshUsers`、`showCreateUserModal`、`createUser`、`showEditUserModal`、`updateUser`、`deleteUser` 等 JS 函数

#### 1. 自我保护与权限边界

- **问题 U1：管理员可禁用自己的账户导致锁死（致命）**
  - `api_update_user` 允许管理员修改任意用户的 `is_active`，**未校验目标是否为当前登录用户**。  
  - 若管理员将自己 `is_active` 置为 0，会话校验（`validate_session` 检查 `u.is_active = 1`）将失败，下次请求即被登出。  
  - 后果：系统失去最后一个有效管理员，无人能重新激活账户，形成管理锁死。

- **问题 U2：管理员可降级自己的角色（高）**
  - 更新接口未禁止"当前用户修改自身角色"。  
  - 管理员可将自己的 `role` 改为 `user`，此后丧失管理权限，且无法通过界面恢复（需数据库直接操作）。

- **问题 U3：删除"最后一个管理员"仅前端拦截，后端未校验（高）**
  - 前端通过 `users.filter(u => u.role === 'admin' && u.is_active == 1).length > 1` 控制删除按钮显隐。  
  - 后端 `api_delete_user` 只校验"不能删自己"，不校验"不能删最后一个管理员"。  
  - 后果：通过直接调用 API 可删除最后一个管理员外的唯一管理员，导致系统无管理员可用。

#### 2. 数据与展示

- **问题 U4：编辑用户时全量拉取用户列表（中）**
  - `showEditUserModal(userId)` 调用 `GET /api/users` 获取全部用户，再在前端 `result.data.find(u => u.id === userId)` 查找目标用户。  
  - 用户量较大时浪费带宽和内存，且无单用户查询接口可复用。

- **问题 U5：用户名/邮箱直接插入 HTML 存在 XSS 风险（中）**
  - `renderUsers` 中 `user.username`、`user.email` 直接拼入模板字符串后 `innerHTML` 渲染。  
  - `deleteUser(${user.id}, '${user.username}')` 中 `user.username` 若含单引号会破坏 JS 字符串，若含 `</script>` 等可引入 XSS。  
  - 需对输出做转义或使用 `textContent`/安全模板。

- **问题 U6：deleteUser 中 username 未转义导致 JS 语法错误（中）**
  - `onclick="deleteUser(${user.id}, '${user.username}')"`，若用户名含 `'` 或 `\`，生成的 onclick 将语法错误，删除确认无法正常执行。

#### 3. 登录历史与参数

- **问题 U7：登录历史 limit 无上限（低）**
  - `api_get_login_history` 中 `limit = request.args.get('limit', type=int, default=100)`，未限制最大值。  
  - 可传入 `limit=999999` 等，造成大结果集查询，影响性能。

#### 4. 其他逻辑

- **问题 U8：软删除后无"恢复"入口（低）**
  - `delete_user` 为软删除（`is_active = 0`），列表仍展示禁用用户。  
  - 编辑时可勾选"激活账户"恢复，但无明确"恢复用户"提示，部分管理员可能误以为需重新创建。

- **问题 U9：用户名大小写一致性未统一（低）**
  - `get_user_by_username` 使用 `WHERE username = %s` 精确匹配，`Admin` 与 `admin` 视为不同用户。  
  - 是否需要在创建/登录时统一大小写，取决于产品策略，建议在文档或校验中明确。

---

## 10. 模型学习页面 — 其他功能逻辑问题（2026-02-12 第二轮审查）

> 覆盖范围：性能评估（基础/综合）、参数优化（标准/在线学习/自适应）、优化历史、定时学习任务。  
> 回测引擎已在前一轮修复，本轮不重复审查。

### 高优先级

- **问题 ML1：创建定时任务 — 星期参数未正确传递（高）**
  - 前端 `createModelLearningTask` 将 `scheduleWeekdays` 放入 `task_config.weekdays`。  
  - 后端 `api_create_scheduled_task` 期望 `schedule_weekdays` 作为顶级字段。  
  - **后果**：每周类型任务的星期配置丢失，任务无法按预期星期执行。  
  - **修复**：前端改为传递 `schedule_weekdays: scheduleWeekdays`，或后端同时从 `task_config.weekdays` 读取。

- **问题 ML2：参数优化无进度反馈（高）**
  - `runParameterOptimization` 只显示"正在优化参数"的 loading 文本。  
  - 页面已有 `optimizationProgress` 进度条组件，但**未与标准优化连接**。  
  - 优化可能耗时数分钟，用户无法判断是卡住还是在运行。  
  - **修复**：后端改为异步+轮询，或前端增加预估时间提示。

- **问题 ML3：综合评估结果 — 数值 NaN 风险（高）**
  - `displayComprehensiveResults` 直接执行 `comp.direction_accuracy * 100`。  
  - 若后端返回 `null`/`undefined`，显示 `NaN%`。同理 `magnitude_accuracy`、`avg_confidence` 等。  
  - **修复**：所有数值计算前加 `(val || 0)` 保护。

- **问题 ML4：所有异步操作缺少按钮防重复点击（高）**
  - 五个主要操作函数均无按钮禁用逻辑，用户可在等待中重复点击导致并发请求。  
  - **修复**：请求发出时 `btn.disabled = true`，finally 中恢复。

### 中优先级

- **问题 ML5：性能评估 — 字号覆盖未重置（中）**
  - "无数据"时将指标卡片 `fontSize` 设为 `20px`，有数据时未重置回 `32px`。  
  - **修复**：有数据分支中显式设置 `el.style.fontSize = '32px'`。

- **问题 ML6：综合评估 — 全部子评估失败时无提示（中）**
  - 三个 `if` 条件可能全部不满足，此时显示空白容器。  
  - **修复**：末尾加兜底提示"暂无足够数据进行综合评估"。

- **问题 ML7：定时任务列表 — XSS 注入风险（中）**
  - `task.task_name`、`task.last_run_message` 直接拼入 `innerHTML`。  
  - **修复**：使用文本转义函数处理用户可控字段。

- **问题 ML8：定时任务 — 状态显示逻辑混乱（中）**
  - 前端用 `task.is_active == 1` 松散比较判断启停，后端返回类型不确定。  
  - 状态颜色用 `last_run_status`（上次运行结果）而非 `is_active`（启停状态），含义混淆。  
  - **修复**：统一用 `is_active` 判断启停，`last_run_status` 仅展示上次结果。

- **问题 ML9：创建任务模态框 — 关闭选择器不精确（中）**
  - 用 `querySelector('div[style*="position: fixed"]')` 关闭模态框，可能误关其他元素。  
  - **修复**：保存模态框引用变量，关闭时直接 `modal.remove()`。

- **问题 ML10：后端 API 参数验证不足（中）**
  - `days` 参数未校验范围（负数、0、超大值均通过）。  
  - **修复**：添加 `days = max(1, min(days, 365))` 范围限制。

- **问题 ML11：创建任务弹窗 — 任务类型描述过时（中）**
  - 弹窗选项仍写"参数优化（优化权重参数）"，实际优化的是交易参数。  
  - **修复**：改为"参数优化（优化交易参数：买卖阈值/止盈止损等）"。

### 低优先级

- **问题 ML12：前端 fetch 未检查 HTTP 状态码（低）**
  - 所有 `fetch` 未检查 `response.ok`，非 200 响应时可能抛出非预期异常。  
  - **修复**：解析前加 `if (!response.ok) throw new Error(...)` 检查。

- **问题 ML13：应用优化参数后清除了结果展示（低）**
  - 成功后隐藏结果区域，用户无法回顾。  
  - **修复**：保留结果显示，仅禁用按钮并标记"已应用"。

- **问题 ML14：性能评估后端 SQL 查询无 LIMIT（低）**
  - 预测记录极多时可能导致内存问题。  
  - **修复**：添加合理的 LIMIT。

- **问题 ML15：后端 float() 转换异常未逐一捕获（低）**
  - 非数字字符串会导致整个评估失败。  
  - **修复**：使用安全转换函数。

- **问题 ML16：日期解析用裸 except（低）**
  - `except: continue` 会捕获 `KeyboardInterrupt` 等不应捕获的异常。  
  - **修复**：改为 `except (ValueError, TypeError): continue`。

---

## 后续计划

1. **全部模块已完成审查（含第二轮模型学习深审 + 第三轮回测/优化器深审）**。

2. 后续可进行的工作：  
   - 按"优先级 + 工作量"制定**分阶段修复路线图**，方便分批次落地改动  
   - 针对高优先级问题单独整理"推荐改动方案"文档

---

## 11. 回测引擎 + 参数优化器 — 第三轮深度审查与修复（2026-02-12）

> 覆盖范围：`backtest_engine.py`（backtest_predictions / backtest_with_parameters）、`model_optimizer.py`（optimize_weights_bayesian）

### 致命

- **BT2+OPT1：优化->回测闭环断裂（已修复）**
  - `backtest_predictions` 硬编码止盈8%/止损-5%/仓位30%/买卖阈值，优化器找到的最佳参数无法在用户回测中生效。
  - 修复：新增 `_get_trading_params_from_config()` 从数据库配置读取参数；买入改用 `_buy_stock_with_max_position`；返回结果增加 `parameters` 字段。

### 高优先级

- **BT1：风控使用前一天旧价格（已修复）**
  - 日切时先做风控再处理当天预测，导致 `latest_prices` 还是前一天的。
  - 修复：两个回测函数均新增 `date_prices_map` 预处理，日切时先更新当天价格再风控。

- **BT4：贝叶斯交叉验证必崩（已修复）**
  - `model_optimizer.py` 验证集回测使用未定义变量 `normalized_params`。
  - 修复：改为 `params`。

### 中优先级

- **OPT2：基线对比不公平（已修复）**
  - `current_prediction_config` 可能缺少交易参数键，基线回测全用默认值。
  - 修复：贝叶斯优化前补全交易参数默认值。

- **BT3：风控未传 exclude_symbols 和 max_holding_days（已顺带修复）**
  - `backtest_predictions` 的 `_check_positions_for_sell` 调用缺少参数。
  - 修复：补全参数传递。

---

## 12. 全模块第四轮深度代码审查（2026-02-12）

> 覆盖范围：模型训练、备份管理、新闻抓取、定时任务管理、用户管理、在线学习/自适应学习  
> 审查方式：逐文件阅读核心函数代码，对照第一轮记录的问题确认是否仍存在

### 审查结论：第一轮记录的问题绝大多数仍然存在（未修复）

以下列出每个模块**确认仍存在**的问题和**新发现**的问题：

#### 模型训练 (ml_model_trainer.py + web_app.py 训练API)

| 问题 | 严重度 | 状态 |
|------|--------|------|
| 训练API无并发控制，可重复启动同类型任务 | 高 | 未修复 |
| ml_training_tasks 字典只增不减，长期运行膨胀 | 高 | 未修复 |
| 训练进度 0→5→100 无中间更新 | 中 | 未修复 |
| _calculate_logloss 裸 except（第361行） | 低 | 未修复 |

#### 备份管理 (backup_manager.py)

| 问题 | 严重度 | 状态 |
|------|--------|------|
| 扫描备份无法恢复/删除（前端传ID，扫描备份无ID） | 严重 | 未修复 |
| 备份ID用列表长度+1生成，删除后会冲突 | 严重 | 未修复 |
| 元数据读写无文件锁，并发不安全 | 中 | 未修复 |

#### 新闻抓取 (news_task_manager.py)

| 问题 | 严重度 | 状态 |
|------|--------|------|
| delete_task 只删 news_crawl_tasks 不删 scheduled_tasks | 致命 | 未修复 |
| update_task 只更新旧表不更新统一调度表 | 致命 | 未修复 |
| hourly 模式忽略 interval_minutes | 高 | 未修复 |

#### 定时任务管理 (scheduled_task_manager.py)

| 问题 | 严重度 | 状态 |
|------|--------|------|
| get_task 只查 scheduled_tasks 表 | 严重 | 未修复 |
| 前端操作不传 task_source（后端已支持但前端未适配） | 严重 | 未修复 |
| create_task 中 task_config_json 被赋值两次（新发现，冗余） | 低 | 未修复 |
| get_task 中 JSON 解析用裸 except（新发现） | 低 | 未修复 |

#### 用户管理 (user_manager.py)

| 问题 | 严重度 | 状态 |
|------|--------|------|
| 管理员可禁用自己导致系统锁死 | 致命 | 未修复 |
| 管理员可降级自己角色 | 高 | 未修复 |
| 删除最后一个管理员无后端校验 | 高 | 未修复 |

#### 在线学习/自适应 (online_learning.py + adaptive_learning_integration.py)

| 问题 | 严重度 | 状态 |
|------|--------|------|
| 在线学习更新"因子权重"而非"交易参数"，与回测/优化体系脱节（新发现） | 中 | 设计问题 |
| 自适应优化调用 grid_search 而非 bayesian（新发现） | 中 | 未修复 |

### 总结

- 本轮审查**未发现新的致命级代码bug**（之前修复的回测/优化器闭环问题仍是最关键的修复）
- 第一轮记录的问题（用户管理自我保护、新闻抓取双表不同步等）**均确认仍然存在**
- 新发现2个设计层面问题：在线学习体系与交易参数体系脱节、自适应优化未用贝叶斯
- 这些问题不影响当前核心功能（回测+优化+预测）的正确性，但影响其他模块的健壮性和安全性

---

## 13. 第四轮审查问题修复记录（2026-02-12）

基于第四轮全模块深度审查发现的问题，按优先级逐一修复。

### 13.1 【致命】用户管理 — 管理员自我保护（web_app.py）
- `api_update_user`：新增 `is_self` 判断，管理员不能将自己角色降级为 user，不能禁用自己账户
- `api_delete_user`：新增活跃管理员计数检查，当目标用户是最后一个活跃管理员时拒绝删除

### 13.2 【致命】新闻抓取 — delete/update 同步统一调度表（news_task_manager.py）
- `update_task()`：更新 `news_crawl_tasks` 后，同步更新 `scheduled_tasks` 表中 `task_type='news_crawl'` 且 `id` 相同的记录（task_name / is_active）
- `delete_task()`：删除 `news_crawl_tasks` 后，同步删除 `scheduled_tasks` 表中对应记录
- 同步操作 try-catch 包裹，失败不影响主流程（非致命降级）

### 13.3 【严重】备份管理 — 扫描备份可操作 + ID 冲突修复（backup_manager.py）
- `create_backup()`：ID 生成从 `len(list)+1` 改为 `max(ids)+1`，防止删除备份后 ID 冲突
- `_scan_backup_files()`：扫描到的新备份文件自动写入 metadata 并持久化，使扫描备份可通过 ID 进行删除/恢复操作；去重逻辑避免与已有 metadata 条目重复

### 13.4 【高】模型训练 — 并发控制 + 任务清理（web_app.py）
- 新增 `_has_running_ml_task()` 函数：检查是否有运行中的训练任务
- 新增 `_cleanup_ml_training_tasks()` 函数：自动清理超过 20 条的已完成/失败历史任务，防止 `ml_training_tasks` 字典内存泄漏
- `api_train_base_model` 和 `api_train_finetune_model`：启动前检查并发，已有运行中任务时返回 409 拒绝

### 13.5 【中】自适应优化改用贝叶斯（adaptive_learning_integration.py）
- `execute_adaptive_optimization()`：优先调用 `optimize_weights_bayesian`（效率更高、搜索空间更优），不可用时自动回退到 `optimize_weights_grid_search`

---

## 14. 第四轮深度审查（预测精准度导向的跨功能逻辑推理，2026-02-13）

> 审查目标：不是只看“功能是否可用”，而是验证`配置 -> 预测 -> 学习 -> 优化 -> 回测验证`是否构成“能持续提升预测精准度”的闭环。  
> 审查范围：`stock_predictor.py`、`prediction_config_manager.py`、`backtest_engine.py`、`model_optimizer.py`、`model_parameter_updater.py`、`online_learning.py`、`adaptive_learning_integration.py`、`web_app.py`、`templates/settings.html`。

### 14.1 关键结论（先给结论）

1. 当前系统已经具备“预测 + 回测 + 在线学习 + 自适应优化”的模块化能力，但**提升预测精准度的主闭环仍不完整**。  
2. 现状更偏向“优化交易执行参数（择时/风控）”，而不是“稳定提升预测本身准确率（方向/概率校准）”。  
3. 存在多个跨模块语义断点：**参数命名不一致、分类不一致、单位不一致、应用链断裂**，会导致“界面已配置/已优化，但预测链路未真正生效”。

---

### 14.2 高优先级逻辑断点（按对“预测精准度”的影响排序）

#### P1【致命】优化结果“可算出但难落地”：优化应用链与配置校验冲突

- `ModelOptimizer` 当前优化的是交易参数（`buy_threshold/sell_threshold/min_confidence/stop_loss_pct/take_profit_pct/max_position_pct`）。  
- `ModelParameterUpdater.apply_optimized_parameters()` 在创建新配置时，把 `new_parameters` 直接放入 `values['prediction']`。  
- 但 `PredictionConfigManager.validate_config()` 要求 `prediction` 下 5 个传统权重之和必须约等于 1.0。  
- 当 `new_parameters` 只包含交易参数、不含 5 个权重时，权重和为 0，校验失败，导致“优化结果无法稳定激活”。

**影响到精准度的方式**：优化过程与线上生效断裂，系统无法形成“评估 -> 优化 -> 应用 -> 再验证”的持续改进闭环。

#### P2【致命】回测读取交易参数的分类路径错误，导致大量用户配置不生效

- `BacktestEngine._get_trading_params_from_config()` 从 `config['prediction']`（或 `prediction.trading`）取交易参数。  
- `PredictionConfigManager.get_config()` 返回的是并列分类：`prediction`、`indicator`、`news`、`trading`。  
- 这会导致 `trading` 分类中的参数（尤其止损/止盈/仓位）在回测时常被忽略并回退默认值。

**影响到精准度的方式**：回测评估基于“错误参数”，反馈信号失真，进一步误导后续参数迭代。

#### P3【高】交易参数语义分裂：UI键名与回测键名不一致

- 设置页交易参数以 `buy_signal_threshold/sell_signal_threshold/strong_buy_threshold/...` 为主；  
- 回测/优化核心使用 `buy_threshold/sell_threshold/min_confidence/...`；  
- 两套键名并行、缺少统一映射，导致“用户看起来改了参数，核心回测/优化未必读到同一组参数”。

**影响到精准度的方式**：配置意图无法准确传递到核心引擎，实验结果不可重复，调参结论不可靠。

#### P4【高】传统权重与ML权重语义冲突（0.65/0.35 vs 校验=1.0）

- `config.py` 语义是“传统因子总权重约 0.65 + ML权重约 0.35”；  
- 但 `validate_config()` 强制传统5因子权重和约等于 1.0；  
- `StockPredictor` 又会把“传统权重 + ml_weight”二次归一化。  

**实际后果**：如果传统因子按 1.0 配置，再叠加 ML 0.35，归一化后 ML 实际占比约 26%（而非设计目标 35%）。  
这会直接改变模型融合结构，影响方向概率和置信度分布。

#### P5【高】在线学习默认“只给建议不落库”，自动学习链路默认中断

- `online_learning.batch_update_weights()` 仅返回 `new_weights`，默认不保存。  
- `AdaptiveLearningIntegration.execute_online_learning_batch()` 也只是返回结果。  
- 需要额外调用 `/api/model/apply-online-learning-weights` 才会创建并激活新配置。  

**影响到精准度的方式**：线上“学到的权重”无法自动进入下一轮预测，学习系统难以持续自我改进。

#### P6【中高】自适应优化自动应用阈值单位疑似错误（0.05 被当 5%）

- `adaptive_learning_integration.py` 传 `auto_apply_threshold: 0.05`，注释写“5%改进自动应用”。  
- 优化器内 `improvement_pct` 是百分数口径（例如 3.2 表示 3.2%），与阈值直接比较。  
- 这会把门槛从预期 5% 降到 0.05%，几乎“微小波动即自动应用”。

**影响到精准度的方式**：参数频繁抖动，模型行为不稳定，容易过拟合近期噪声。

---

### 14.3 预测精准度视角下的“闭环状态图”

**现状可运行链路：**
- 数据采集 -> 生成预测 -> 入库 -> 回测评估 -> 产生优化候选

**当前断裂点：**
- 候选参数应用（校验冲突）  
- 交易参数读取（分类/键名错位）  
- 在线学习结果自动落地（默认断开）  
- 权重语义一致性（0.65/0.35 与 1.0 校验冲突）

**结论**：系统“有学习能力”，但“难以稳定生效到下一轮预测”。

---

### 14.4 面向“提升预测精准度”的修复优先级建议（执行顺序）

1. **先修统一语义层（P1+P2+P3+P4）**  
   - 统一参数主模型：明确哪些属于“预测参数”、哪些属于“交易执行参数”；  
   - 统一键名（至少在服务端建立兼容映射）；  
   - 统一权重口径（传统权重是 0.65 还是 1.0，二选一并全链路一致）。

2. **再修自动学习落地层（P5+P6）**  
   - 在线学习支持可控自动应用（阈值+回滚）；  
   - 修正自适应自动应用阈值单位，避免高频参数抖动。

3. **最后做精准度验证层**  
   - 增加“配置版本 -> 预测批次 -> 评估结果”的版本追踪；  
   - 用同一配置版本做 A/B 对照（新旧版本方向准确率、置信度校准误差）。

---

### 14.5 第四轮审查总评（预测精准度导向）

- 当前系统的主要问题不是“没有算法”，而是“跨模块语义未对齐”，导致学习和优化成果难以稳定转化为更高预测准确率。  
- 只要先打通语义一致性与参数生效闭环，再做学习策略微调，预测精准度会比继续堆新功能提升更快、更可控。

### 14.6 最小改动落地方案（建议先做这4步）

#### Step 1：修复“交易参数读取路径”并统一单位（优先级最高）

- 修改点：`utils/backtest_engine.py::_get_trading_params_from_config`  
- 目标：优先读取 `config['trading']`，兼容读取 `config['prediction']` 中历史字段。  
- 同时统一 `max_position_pct` 语义（`0.3` 比例 vs `30` 百分数），在读取处做一次标准化：`>1` 则除以 `100`。  
- 预期：回测使用的就是用户设置页生效参数，不再频繁回退默认值。

#### Step 2：修复“优化参数应用失败”链路

- 修改点：`utils/model_parameter_updater.py::apply_optimized_parameters`  
- 目标：不要把交易参数直接塞进 `values['prediction']`；应写入 `values['trading']`，并保留当前 `values['prediction']` 的权重字段。  
- 补充：`PredictionConfigManager.validate_config()` 不应因“本次仅更新交易参数”而失败；可支持“prediction权重未改动时跳过权重和校验”或“从旧配置补齐权重后再校验”。  
- 预期：`/api/model/apply-optimization/*` 可稳定成功，形成“优化 -> 激活 -> 下一轮回测验证”闭环。

#### Step 3：建立交易参数键名兼容映射（UI <-> 引擎）

- 修改点：`utils/prediction_config_manager.py`（保存/读取层）或 `web_app.py`（API入参层）  
- 建议映射（兼容旧字段）：
  - `buy_signal_threshold -> buy_threshold`
  - `sell_signal_threshold -> sell_threshold`（注意语义是“上涨概率上限”还是“下跌概率阈值”，需在文档中写清）
  - `trading.min_confidence -> min_confidence`
- 预期：设置页改动能直接体现在回测/优化引擎，减少“改了但没生效”的错觉。

#### Step 4：修正自动应用阈值单位，防止参数抖动

- 修改点：`utils/adaptive_learning_integration.py`  
- 把 `auto_apply_threshold` 从 `0.05` 改为 `5.0`（若优化器按百分数比较）。  
- 预期：仅在有实质改进时自动应用，降低高频微调导致的过拟合风险。

---

### 14.7 验收标准（以“预测精准度提升”而非“功能可用”为准）

1. **生效一致性验收**  
   - 在设置页修改交易参数后，回测日志打印值与设置值一致（允许小数格式差异，不允许语义差异）。

2. **闭环可用性验收**  
   - 参数优化后点击“应用优化参数”成功率应接近100%，并可在配置列表看到新激活配置。

3. **稳定性验收**  
   - 自适应优化自动应用频率显著下降（从“几乎每次都应用”降到“仅明显提升时应用”）。

4. **精准度验收（核心）**  
   - 固定股票池 + 固定评估窗口下，新配置相较旧配置：  
     - 方向准确率（direction_accuracy）提升，或  
     - 在方向准确率持平时，置信度校准误差下降（更可靠的概率输出）。

---

## 15. 设置页面--日志更新（2026-02-13）

> 目标：记录本轮已落地修改，便于后续追踪“是否真的提升预测精准度”。

### 15.1 本轮已完成代码修改

1. **回测交易参数读取逻辑修复**（`utils/backtest_engine.py`）
   - `_get_trading_params_from_config()` 改为优先读取 `config['trading']`；
   - 兼容历史路径 `config['prediction']` / `prediction.trading`；
   - 增加键名兼容：`buy_signal_threshold`、`sell_signal_threshold`、`trading_min_confidence`、`confidence_threshold`、`max_single_position_pct`；
   - 增加单位标准化：`max_position_pct > 1` 自动按百分比转比例；
   - 增加防御性处理：`stop_loss_pct` 误为正值时自动转负值。

2. **优化参数应用链路修复**（`utils/model_parameter_updater.py`）
   - 新增 `_extract_trading_params()`，统一提取和标准化交易参数；
   - `apply_optimized_parameters()` 不再把优化结果直接写入 `prediction`，改为写入 `trading` 并保留原 `prediction` 权重结构；
   - 为兼容旧读取逻辑，同步关键交易参数到 `prediction`（兼容层，不改变主语义）。

3. **优化参数回滚链路对齐修复**（`utils/model_parameter_updater.py`）
   - `rollback_parameters()` 与应用链路保持一致：回滚交易参数到 `trading`，并同步兼容字段到 `prediction`；
   - 避免回滚时再次触发“权重校验冲突”或“回滚后交易参数不生效”。

4. **自适应优化自动应用阈值单位修复**（`utils/adaptive_learning_integration.py`）
   - `auto_apply_threshold` 从 `0.05` 修正为 `5.0`（与 `improvement_pct` 百分数口径一致）；
   - 降低微小改进触发自动应用造成的参数抖动风险。

### 15.2 本轮未改但已确认需后续跟进

- `PredictionConfigManager.validate_config()` 仍采用“prediction权重和=1.0”强校验。  
  当前通过“保留原 prediction 权重结构”绕过冲突；后续建议升级为更清晰的“按变更分类校验”。

### 15.3 快速验证状态

- 已完成相关文件语法检查（`py_compile`）通过；
- 本轮未引入新的 lint 报错（已检查改动文件）。

### 15.4 自动化回归验证（2026-02-13）

> 说明：本轮先做“代码级自动化验证”（不依赖手工点页面），用于确认核心修复逻辑已生效。

1. **backtest 参数映射与单位标准化：通过**
   - 验证点：
     - `buy_signal_threshold -> buy_threshold`
     - `sell_signal_threshold -> sell_threshold`
     - `confidence_threshold -> min_confidence`
     - `max_single_position_pct=35 -> max_position_pct=0.35`
     - `stop_loss_pct=+6 -> -6`
   - 结果：全部 PASS。

2. **backtest 读取优先级（trading 优先于 prediction）：通过**
   - 验证点：同键同时存在时，优先读取 `trading` 分类值。
   - 结果：PASS（读取到 `trading.buy_threshold=0.69`）。

3. **apply/rollback 交易参数提取兼容：通过**
   - 验证点：
     - 兼容旧键名提取交易参数；
     - `max_single_position_pct=40` 归一化为 `0.4`；
     - `stop_loss_pct=+5` 归一化为 `-5`。
   - 结果：全部 PASS。

4. **自适应自动应用阈值单位：通过**
   - 验证点：`adaptive_learning_integration.py` 中阈值是否为 `5.0` 且不再出现 `0.05`。
   - 结果：PASS（命中 `5.0`，`0.05` 为 0 次）。

### 15.5 下一步验证建议（接口级）

- 建议补做一次 API 级验证（需启动服务）：
  1. `/api/config/*` 保存交易参数后，`/api/backtest/run` 日志是否读取一致；
  2. `/api/model/apply-optimization/<id>` 是否稳定成功并激活新配置；
  3. `/api/model/adaptive-optimization` 在小幅改进下是否不再自动应用。

### 15.6 接口级验证阻塞记录（2026-02-13）

- 已尝试执行：`py -3 -m pytest smart_stock_advisor/test_api_endpoints.py -q`。  
- 结果：**1 passed, 3 errors**，错误原因为测试文件依赖 `session` fixture，但当前测试工程未提供该 fixture（历史问题，非本轮改动引入）。
- 结论：当前仓库内置接口测试脚本不可直接作为回归基线，需要先补齐测试基建（fixture/登录会话管理）后再做全自动 API 回归。

### 15.7 接口测试基建修复与复测结果（2026-02-13）

1. **测试基建修复**
   - 修改文件：`test_api_endpoints.py`
   - 新增 `pytest` 会话级 `session` fixture（统一登录会话）；
   - 登录失败时改为 `pytest.skip`，避免 fixture 缺失导致的硬错误；
   - 清理测试函数返回值写法（改为断言/失败），避免 `PytestReturnNotNoneWarning`；
   - 更新 `test_validate_config` 的有效/无效样本，使其匹配当前“5因子权重和=1.0”的校验规则。

2. **复测结果**
   - 执行命令：`py -3 -m pytest smart_stock_advisor/test_api_endpoints.py -q`
   - 结果：**4 passed**

3. **说明**
   - 当前仍有 `pytest-cov` 的覆盖率插件警告（No data collected），属于测试覆盖率配置问题，不影响本轮接口回归结论。

### 15.8 真实业务链路回归（API 冒烟，2026-02-13）

> 验证目标：`参数配置保存 -> 激活配置 -> 回测执行 -> 恢复原配置` 全链路可执行且不破坏现场。

1. **执行步骤**
   - 登录：`/login`
   - 读取当前激活配置：`/api/prediction/config/active`
   - 基于激活配置创建临时配置（修改 trading 参数）：
     - `buy_signal_threshold=0.68`
     - `sell_signal_threshold=0.36`
     - `min_confidence=0.57`
     - `max_position_pct=25`
     - `stop_loss_pct=-6`
     - `take_profit_pct=12`
   - 激活临时配置：`/api/prediction/config/<id>/activate`
   - 运行回测：`/api/backtest/run`（最近7天，关闭可视化）
   - 恢复原激活配置并删除临时配置

2. **实际结果**
   - `login_status=200`
   - `original_active_id=10`
   - `new_config_id=11`
   - `activate_temp=ok`
   - `backtest_success=True`
   - `restore_original=True`
   - `delete_temp=True`

3. **结论**
   - 本轮修复后，配置创建/激活/回测/恢复链路可完整跑通；
   - 关键是“回测阶段没有因交易参数分类/键名问题报错或失效”。

### 15.9 优化参数应用接口现状（2026-02-13）

- 当前优化历史：`id=2`（`improvement_pct=-32.47`，未应用）、`id=1`（已应用）。  
- 因现有未应用记录为负改进，`/api/model/apply-optimization/<id>` 按设计应拒绝（`force=False`），属于正常保护行为。  
- 后续建议：先生成一条正改进优化记录，再做“应用优化参数”成功路径的在线回归验证。

---

## 16. 第六轮逻辑审查（设置页面-股票预测相关功能，2026-02-13）

> 审查范围：`panel-params`、`panel-model-learning`、回测/优化/在线学习相关前后端链路。  
> 审查目标：识别“设置了但不生效、评估闭环断裂、参数语义不一致”这三类会直接影响预测精准度的逻辑问题。

### 16.1 核心结论

- 本轮发现 **4 个高优先级逻辑断点 + 1 个中优先级断点**。  
- 其中最关键的是：**参数优化前后端协议已不一致，设置页“执行优化”当前无法得到有效结果展示与应用ID**。  
- 当前系统在“回测链路”已基本可跑通，但“设置页优化闭环”和“参数语义一致性”仍会显著影响预测优化效率。

### 16.2 高优先级问题

#### R6-1【高】参数优化前后端协议不一致，设置页优化结果链路断裂

- 前端 `templates/settings.html` 的 `runParameterOptimization()` 期望 `/api/model/optimize` 直接返回 `optimization_id`、`best_score`、`current_score`、`improvement_pct` 等结果并立即渲染。  
- 但后端 `web_app.py` 的 `/api/model/optimize` 当前只返回 `progress_id`（异步模式），不返回优化结果。  
- 已实际验证接口响应：`{"success":true,"progress_id":"..."}`。  

**影响**：设置页“执行优化”后无法按当前前端逻辑进入“展示结果 -> 应用优化参数”的完整路径，优化闭环在UI层断裂。

#### R6-2【高】权重引导文案/交互与后端校验冲突（0.65 vs 1.0）

- 设置页仍引导“5个传统因子合计 0.65，ML 0.35”，且“一键平衡”会把传统因子强制调到 0.65。  
- 后端 `PredictionConfigManager.validate_config()` 仍要求传统5因子权重和约等于 1.0。  

**影响**：用户按页面引导操作后，保存校验会失败或长期处于“前端看似合理、后端拒绝”的状态，直接阻断参数调优。

#### R6-3【高】设置页大量 `value || default`，导致合法 `0` 值被覆盖

- 在 `loadConfigValues()` 与 `getCurrentConfigValues()` 中，大量字段使用 `||` 回退默认值。  
- 对允许为 `0` 的参数（如 `ml_min_weight`、异常降权比例、部分阈值）会被错误替换为默认值。  

**影响**：用户无法稳定保存“0值策略”；参数会在加载/保存过程中被静默改写，导致预测行为与配置意图不一致。

#### R6-4【高】`performance` 分类仍未从后端读取，设置页性能参数存在“保存后读不回”风险

- `prediction_config_manager.get_config()` 构造 `config_dict` 时仍只包含：`prediction/indicator/news/trading`，缺少 `performance`。  
- 设置页却会读取并提交 `performance` 字段。  
- 实际接口验证当前激活配置返回 keys 也不含 `performance`。  

**影响**：性能相关参数可能出现“实际已存储但页面不回显，下一次保存又被默认值覆盖”的行为；间接影响预测执行效率与稳定性。

### 16.3 中优先级问题

#### R6-5【中】`max_position_pct` 百分比/比例单位在设置页展示层仍不统一

- 设置页输入控件按“百分数”设计（如 30 表示30%），  
- 优化应用链路中已支持比例写入（如 0.3），后端回测读取也会做标准化。  
- 但设置页加载展示未统一转换，可能出现显示 `0.3`（用户会理解为 0.3%）的混淆。

**影响**：用户误判仓位参数，导致后续手工调参方向错误，影响回测和实盘一致性认知。

### 16.4 第六轮修复优先级建议

1. **先打通优化UI闭环（R6-1）**  
   - 二选一：  
   - A) 前端改为“启动优化 -> 轮询 progress_id -> 拉取最终结果”；  
   - B) 后端改回同步返回优化结果（不推荐，阻塞风险高）。

2. **统一权重语义（R6-2）**  
   - 页面文案、平衡逻辑、后端校验三者必须统一为同一口径（建议统一到 1.0）。

3. **替换 `||` 为空值安全判断（R6-3）**  
   - 数值参数统一使用 `Number.isFinite()` 或 `??` 语义，确保 `0` 不被误改写。

4. **补齐 `performance` 读取分类（R6-4）**  
   - 后端 `get_config()` 返回中加入 `performance`，避免回显丢失和二次覆盖。

5. **统一仓位单位展示（R6-5）**  
   - 设置页展示层统一按百分数显示，保存时再转换为内部标准单位（建议统一内部比例制）。

---

## 17. 第六轮问题修复记录（2026-02-13）

### 17.1 修复内容

#### R6-1【高】参数优化前后端协议不一致 - 已修复 ✅
- **问题**：前端期望同步返回优化结果，但后端只返回 `progress_id`（异步模式）
- **修复**：
  - 后端：在 `model_optimizer.py` 中，优化完成时将完整结果存储到进度中（包含 `optimization_id`、`best_score`、`improvement_pct` 等）
  - 前端：修改 `runParameterOptimization()` 函数，改为轮询模式：
    - 启动优化后获取 `progress_id`
    - 每2秒轮询 `/api/model/optimize/progress/<progress_id>` 获取进度
    - 当状态为 `completed` 时，从进度中提取完整结果并显示
  - 新增API：`GET /api/model/optimize/progress/<progress_id>` 用于查询优化进度

#### R6-2【高】权重引导文案与后端校验冲突 - 已修复 ✅
- **问题**：前端引导"传统因子合计0.65，ML模型0.35"，但后端校验要求传统因子权重和为1.0
- **修复**：
  - 前端文案改为"传统因子权重合计应为1.0（ML模型权重在预测时动态计算）"
  - `updateWeightSum()` 函数改为检查权重和是否为1.0（±0.01容差）
  - `balanceWeights()` 函数改为将传统因子权重和调整为1.0
  - 默认配置加载时，`history_weight` 调整为0.40，使总和为1.0

#### R6-3【高】`value || default` 导致合法0值被覆盖 - 已修复 ✅
- **问题**：大量使用 `value || default`，导致合法的0值被错误替换为默认值
- **修复**：
  - 新增辅助函数 `_safeGetNumber(value, defaultValue)`：使用 `!== undefined && !== null` 判断，支持0值
  - `loadConfigValues()` 中所有数值加载改为使用 `_safeGetNumber()` 或 `safeNumber()` / `safeInt()`
  - `getCurrentConfigValues()` 中所有数值获取改为使用 `isNaN()` 判断，而非 `||`
  - `getElementValue()` 函数改为 `isNaN(val) ? defaultValue : val`，支持0值

#### R6-4【高】`performance` 分类未从后端读取 - 已修复 ✅
- **问题**：`get_config()` 只包含4个分类，缺少 `performance`，导致性能参数保存后读不回
- **修复**：
  - `prediction_config_manager.py` 的 `get_config()` 方法中，`config_dict` 初始化时添加 `'performance': {}`
  - 文档字符串更新，说明返回格式包含 `performance` 分类

#### R6-5【中】`max_position_pct` 单位不统一 - 已修复 ✅
- **问题**：设置页输入按百分数（30表示30%），但后端可能存储为比例（0.3），导致显示混淆
- **修复**：
  - 加载时：`loadConfigValues()` 中，如果 `max_position_pct <= 1`，则乘以100转换为百分数显示
  - 保存时：`getCurrentConfigValues()` 中，如果 `max_position_pct > 1`，则除以100转换为比例格式保存
  - 统一前端展示为百分数格式（30表示30%），后端存储为比例格式（0.3）

### 17.2 修复文件清单

1. **后端文件**：
   - `web_app.py`：新增 `/api/model/optimize/progress/<progress_id>` 接口
   - `utils/model_optimizer.py`：优化完成时将完整结果存储到进度中
   - `utils/prediction_config_manager.py`：`get_config()` 添加 `performance` 分类

2. **前端文件**：
   - `templates/settings.html`：
     - `runParameterOptimization()` 改为轮询模式
     - 权重引导文案和校验逻辑统一为1.0
     - 所有数值加载/保存改为支持0值的安全函数
     - `max_position_pct` 单位统一处理

### 17.3 验证建议

1. **R6-1验证**：执行参数优化，确认能通过轮询获取并显示完整结果
2. **R6-2验证**：设置传统因子权重，确认前端校验和后端校验一致（权重和=1.0）
3. **R6-3验证**：将某个参数设置为0，保存后重新加载，确认0值未被覆盖
4. **R6-4验证**：修改性能参数，保存后重新加载，确认参数能正确读取
5. **R6-5验证**：设置 `max_position_pct` 为30，保存后重新加载，确认显示为30而非0.3

### 17.4 第六轮“功能逻辑深度审查与预测精准度”在本处完成（2026-02-21）

- **R6-1～R6-5**：代码中已实现（轮询优化进度、权重 1.0 统一、0 值安全、performance 读取、max_position_pct 单位统一）。
- **本处补充修改**：
  1. **R6-2 残留**：将设置页仍写「0.65」的 3 处文案改为「1.0」  
    - 第一区说明：「传统因子合计必须等于 **1.0**」  
    - 验证配置说明：「权重是否等于 **1.0**」  
    - 一键平衡说明：「使其精确合计等于 **1.0**」
  2. **R6-3 残留**：`getCurrentConfigValues()` 中 performance 的 5 个数值字段由 `parseInt(...) || default` 改为 `getElementValue(id, default, parseInt)`，避免合法 0 被默认值覆盖。
- **涉及文件**：`templates/settings.html`
- **建议自测**：按 17.3 的 5 条验证建议做一次回归；重点检查权重文案与保存/加载 0 值、performance 参数。

---

## 18. 第七轮深度逻辑审核（回测功能，2026-02-21）

> 审查目标：回测功能是否符合要求，是否需要扩展以让用户更直观地理解结果与参数。  
> 审查范围：`utils/backtest_engine.py`、`web_app.py` 中 `/api/backtest/*`、`templates/settings.html` 回测区域、`visualizer/backtest_visualizer.py`。

### 18.1 核心结论

- **回测链路**：从“选择日期 → 执行回测 → 展示指标 → 查看可视化报告”已闭环可用，交易参数从当前激活配置读取（与第三轮/第五轮修复一致），**符合当前对“用当前参数做历史模拟”的要求**。
- **直观性不足**：设置页回测结果区未展示“本次使用的交易参数”、缺少日期快捷选项、关键指标（夏普、年化、盈亏比）折叠在“详细指标”内；可视化报告也未展示参数。**建议做适度扩展，让用户更直观地判断“是不是在用我想的那套参数”以及“风险收益结构如何”。**

### 18.2 符合性检查（是否符合要求）

| 项目 | 状态 | 说明 |
|------|------|------|
| 回测使用当前激活配置的交易参数 | ✅ | `backtest_predictions` 通过 `_get_trading_params_from_config()` 读库，与设置页激活配置一致；键名/分类已在第五轮统一。 |
| 买卖逻辑与风控 | ✅ | 日切时先更新当日价格再止盈/止损/超时平仓；成本价与市值分离，收益率计算正确。 |
| 指标量纲与前端展示 | ✅ | 后端 `_calculate_metrics` 返回百分比（如 total_return、max_drawdown、win_rate 为 5.23 表示 5.23%）；前端不再重复 ×100。 |
| 无数据时的反馈 | ✅ | 无预测记录时返回 `success: False` 及明确 message；前端展示失败原因。 |
| 数据覆盖透明度 | ✅ | 返回 `data_coverage`（有效交易日/自然日/覆盖率）；前端有展示。 |
| 交易记录与盈亏原因 | ✅ | 每笔含 date、symbol、action、price、profit、profit_pct、reason；前端表格展示最近 30 条，含盈亏%与原因。 |

**结论**：回测功能在“正确性、参数来源、量纲、无数据与覆盖度”上**符合要求**，无需为“符合要求”做逻辑性修复。

### 18.3 高优先级体验断点（让用户更直观点）

#### R7-1【高】设置页回测结果未展示“本次使用的交易参数”

- **现象**：后端已返回 `parameters`（buy_threshold、sell_threshold、min_confidence、stop_loss_pct、take_profit_pct、max_position_pct、max_holding_days），但 `renderBacktestResults` 未渲染。
- **影响**：用户无法在结果区直接确认“这次回测用的就是当前设置页那套参数”，容易产生“改了参数但不知道回测是否用了”的疑惑。
- **建议**：在回测结果区增加一小块“本次回测参数”展示（可折叠），从 `btData.parameters` 读取并展示关键交易参数及单位说明（如止损-5%、仓位30%）。

#### R7-2【高】缺少日期快捷选择，与“参数优化”不一致

- **现象**：回测仅支持手动选择开始/结束日期；“参数优化”有“最近 30/60/90 天”下拉。
- **影响**：用户对比“回测 vs 优化”时需手工对齐日期，容易选错区间，且操作步骤多。
- **建议**：在回测区增加“最近 7 天 / 30 天 / 90 天”快捷按钮或下拉，与优化区一致；选择后自动填充开始/结束日期。

#### R7-3【中】夏普、年化、盈亏比未在首屏展示

- **现象**：`metrics` 中已有 `sharpe_ratio`、`annual_return`、`profit_loss_ratio`，但仅在“展开详细指标”中展示。
- **影响**：风险收益结构（夏普、年化、盈亏比）对评估策略质量很重要，首屏只有总收益/胜率/最大回撤，信息不完整。
- **建议**：在概览卡片中增加 2～3 个指标卡（如夏普比率、年化收益率、盈亏比），或把“详细指标”改为默认展开/分两行展示。

### 18.4 中优先级与可选扩展

| 项目 | 优先级 | 说明 |
|------|--------|------|
| 可视化报告中展示“本次使用的交易参数” | 中 | `BacktestVisualizer._create_html_report` 当前未使用 `backtest_result.get('parameters')`，报告内无参数说明；可加一小节“交易参数”。 |
| 设置页内嵌简易权益曲线 | 低 | 当前权益曲线仅在 HTML 报告中；若需“不跳转即看趋势”，可在结果区用 `daily_values` 画简易折线（或仅提供“查看报告”链接，保持现状）。 |
| 交易记录导出 CSV | 低 | 便于用户离线分析；可增加“导出交易记录”按钮，前端或后端生成 CSV 下载。 |

### 18.5 第七轮修复优先级建议

1. **R7-1**：在 `renderBacktestResults` 中增加“本次回测参数”展示块（来自 `btData.parameters`）。
2. **R7-2**：在回测表单区增加“最近 7/30/90 天”快捷选项，自动填充开始/结束日期。
3. **R7-3**：在回测结果概览卡片中增加夏普比率、年化收益率、盈亏比（或默认展开详细指标）。

### 18.6 验收标准（回测功能）

- 执行回测后，结果区可见“本次使用的交易参数”（至少包含买卖阈值、止损止盈、仓位、最大持仓天数）。
- 可通过快捷选项一键选择“最近 7/30/90 天”并成功执行回测。
- 首屏或展开区能直接看到夏普比率、年化收益率、盈亏比，无需猜测是否在“详细指标”里。

### 18.7 第七轮修复记录（2026-02-21）

| 项目 | 状态 | 说明 |
|------|------|------|
| **R7-1** 本次回测使用的交易参数 | ✅ | 设置页 `renderBacktestResults` 中增加可折叠块「📋 本次回测使用的交易参数」，从 `btData.parameters` 展示买入/卖出阈值、最小置信度、止损、止盈、单只仓位、最大持仓天数。 |
| **R7-2** 日期快捷 7/30/90 天 | ✅ | 回测表单区增加「7天」「30天」「90天」按钮，`setBacktestDateRange(days)` 自动填充开始/结束日期。 |
| **R7-3** 夏普、年化、盈亏比首屏展示 | ✅ | 结果区概览卡片增加夏普比率、年化收益率、盈亏比三张卡片；详细指标中排除上述三项避免重复。 |
| **可视化报告内交易参数** | ✅ | `BacktestVisualizer` 增加 `_create_params_section(parameters)`，报告中「📋 本次回测使用的交易参数」小节展示与设置页一致的参数表。 |

**涉及文件**：`templates/settings.html`（回测表单、`renderBacktestResults`、`setBacktestDateRange`）、`visualizer/backtest_visualizer.py`（`_create_params_section`、报告模板中 `params_html`）。

---
