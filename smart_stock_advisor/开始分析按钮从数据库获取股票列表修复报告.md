# 开始分析按钮从数据库获取股票列表修复报告

## 一、问题描述

**问题现象**：
- 点击"开始分析"按钮时，仍然从API（`ak.stock_zh_a_spot_em()`）获取股票列表
- 日志显示：`调用 stock_zh_a_spot_em 时遇到网络连接问题`
- 用户希望直接从数据库 `stock_history_data` 表获取股票数据，不需要从API获取

**问题位置**：设置页面 -> 股票分析 -> "开始分析"按钮

## 二、问题分析

### 2.1 根本原因

在 `web_app.py` 的 `run_stock_analysis_task()` 函数中，当 `symbols` 为 `None`（即按数量模式）时：

**修复前**：
```python
else:
    # 按数量模式：从API获取股票列表
    logger.info(f"【获取股票列表】正在获取股票列表（limit={limit}, sort_type={sort_type}）...")
    sort_by_turnover = (sort_type == 'turnover')
    stock_list = data_source.get_all_stock_list(limit=limit, sort_by_turnover=sort_by_turnover, random_sort=(sort_type == 'random'))
    total_count = len(stock_list) if stock_list else 0
    logger.info(f"【获取股票列表】成功获取 {total_count} 只股票")
```

**问题**：
- 调用 `data_source.get_all_stock_list()`，这会调用 `ak.stock_zh_a_spot_em()` API
- 网络连接失败导致无法获取股票列表
- 之前只修复了定时任务的 `_execute_stock_analysis` 方法，但没有修复"开始分析"按钮调用的 `run_stock_analysis_task` 函数

### 2.2 对比定时任务

**定时任务**（`utils/scheduled_task_manager.py` 的 `_execute_stock_analysis`）：
- ✅ 已修复：从数据库获取股票列表
- ✅ 支持按成交额排序
- ✅ 支持按日期过滤

**开始分析按钮**（`web_app.py` 的 `run_stock_analysis_task`）：
- ❌ 未修复：仍然从API获取股票列表

## 三、修复方案

### 3.1 修复位置

**文件**：`web_app.py`
**方法**：`run_stock_analysis_task()`（第2148-2167行）

### 3.2 修复内容

**修复前**：
```python
else:
    # 按数量模式：从API获取股票列表
    logger.info(f"【获取股票列表】正在获取股票列表（limit={limit}, sort_type={sort_type}）...")
    sort_by_turnover = (sort_type == 'turnover')
    stock_list = data_source.get_all_stock_list(limit=limit, sort_by_turnover=sort_by_turnover, random_sort=(sort_type == 'random'))
    total_count = len(stock_list) if stock_list else 0
    logger.info(f"【获取股票列表】成功获取 {total_count} 只股票")
```

**修复后**：
```python
else:
    # 按数量模式：从数据库获取股票列表（避免API调用）
    logger.info(f"【获取股票列表】正在从数据库获取股票列表（limit={limit}, sort_type={sort_type}）...")
    try:
        from utils.db_connection import DatabaseConnection
        db = DatabaseConnection()
        
        # 根据data_date获取有数据的股票列表（优先使用data_date，如果没有则使用target_date）
        filter_date = data_date if data_date else target_date
        if filter_date:
            # 获取指定日期有数据的股票列表
            sql = """
                SELECT DISTINCT symbol, name
                FROM stock_history_data
                WHERE trade_date = %s AND period_type = 'daily'
                ORDER BY symbol
            """
            results = db.execute_query(sql, (filter_date,))
            stock_list = [{'symbol': str(row['symbol']).zfill(6), 'name': row.get('name', '')} for row in results]
            logger.info(f"从数据库获取到 {len(stock_list)} 只股票（日期: {filter_date}）")
        else:
            # 如果没有指定日期，获取所有有数据的股票
            sql = """
                SELECT DISTINCT symbol, name
                FROM stock_history_data
                WHERE period_type = 'daily'
                ORDER BY symbol
            """
            results = db.execute_query(sql)
            stock_list = [{'symbol': str(row['symbol']).zfill(6), 'name': row.get('name', '')} for row in results]
            logger.info(f"从数据库获取到 {len(stock_list)} 只股票（所有日期）")
        
        if not stock_list:
            error_msg = '数据库中暂无股票数据'
            logger.error(f"【错误】{error_msg}")
            with task_lock:
                if task_id in running_tasks:
                    running_tasks[task_id]['status'] = 'failed'
                    running_tasks[task_id]['error'] = error_msg
            return
        
        # 如果指定了limit，限制数量
        if limit and limit > 0:
            stock_list = stock_list[:limit]
        
        # 如果指定了按成交额排序，需要从数据库获取成交额数据
        if sort_type == 'turnover':
            logger.info("按成交额排序（从数据库获取成交额数据）...")
            # 获取成交额数据并排序
            if filter_date:
                sql = """
                    SELECT symbol, amount
                    FROM stock_history_data
                    WHERE trade_date = %s AND period_type = 'daily'
                    ORDER BY amount DESC
                """
                turnover_results = db.execute_query(sql, (filter_date,))
            else:
                # 获取最新日期的成交额数据
                sql = """
                    SELECT symbol, amount
                    FROM stock_history_data
                    WHERE period_type = 'daily'
                    AND trade_date = (SELECT MAX(trade_date) FROM stock_history_data WHERE period_type = 'daily')
                    ORDER BY amount DESC
                """
                turnover_results = db.execute_query(sql)
            
            # 构建成交额映射
            turnover_map = {str(row['symbol']).zfill(6): row.get('amount', 0) or 0 for row in turnover_results}
            
            # 按成交额排序
            stock_list.sort(key=lambda x: turnover_map.get(x['symbol'], 0), reverse=True)
        elif sort_type == 'random':
            # 随机排序
            import random
            random.shuffle(stock_list)
        
        total_count = len(stock_list)
        logger.info(f"【获取股票列表】成功从数据库获取 {total_count} 只股票")
    except Exception as e:
        error_msg = f'从数据库获取股票列表失败: {str(e)}'
        logger.error(f"【错误】{error_msg}")
        import traceback
        logger.error(traceback.format_exc())
        with task_lock:
            if task_id in running_tasks:
                running_tasks[task_id]['status'] = 'failed'
                running_tasks[task_id]['error'] = error_msg
        return
```

### 3.3 按股票代码模式的优化

**修复前**：
```python
if symbols is not None:
    # 按股票代码模式：直接使用提供的股票代码列表
    logger.info(f"【获取股票列表】使用指定的股票代码列表，共 {len(symbols)} 只股票")
    # 将股票代码列表转换为字典格式（包含symbol和name）
    stock_list = []
    for symbol in symbols:
        # 尝试获取股票名称
        stock_info = data_source.get_stock_info(symbol, skip_pe_pb=True)
        stock_name = stock_info.get('name') or stock_info.get('股票简称') or symbol
        stock_list.append({'symbol': symbol, 'name': stock_name})
```

**修复后**：
```python
if symbols is not None:
    # 按股票代码模式：从数据库获取股票名称（避免API调用）
    logger.info(f"【获取股票列表】使用指定的股票代码列表，共 {len(symbols)} 只股票")
    # 将股票代码列表转换为字典格式（包含symbol和name）
    stock_list = []
    try:
        from utils.db_connection import DatabaseConnection
        db = DatabaseConnection()
        # 从数据库获取股票名称
        filter_date = data_date if data_date else target_date
        if filter_date:
            sql = """
                SELECT DISTINCT symbol, name
                FROM stock_history_data
                WHERE symbol IN ({}) AND trade_date = %s AND period_type = 'daily'
            """.format(','.join(['%s'] * len(symbols)))
            results = db.execute_query(sql, symbols + [filter_date])
        else:
            sql = """
                SELECT DISTINCT symbol, name
                FROM stock_history_data
                WHERE symbol IN ({}) AND period_type = 'daily'
            """.format(','.join(['%s'] * len(symbols)))
            results = db.execute_query(sql, symbols)
        
        name_map = {str(row['symbol']).zfill(6): row.get('name', '') for row in results}
        
        for symbol in symbols:
            symbol_padded = str(symbol).zfill(6)
            stock_name = name_map.get(symbol_padded, symbol_padded)
            stock_list.append({'symbol': symbol_padded, 'name': stock_name})
    except Exception as e:
        logger.warning(f"从数据库获取股票名称失败，使用代码作为名称: {str(e)}")
        # 如果数据库查询失败，使用代码作为名称
        for symbol in symbols:
            stock_list.append({'symbol': str(symbol).zfill(6), 'name': str(symbol).zfill(6)})
```

## 四、修复效果

### 4.1 修复前

1. 点击"开始分析"按钮
2. 调用 `data_source.get_all_stock_list()` API
3. 网络连接失败，无法获取股票列表
4. 任务失败

### 4.2 修复后

1. 点击"开始分析"按钮
2. 从数据库 `stock_history_data` 表获取股票列表
3. 根据 `data_date` 或 `target_date` 过滤有数据的股票
4. 支持按成交额排序或随机排序
5. 任务成功执行

## 五、功能说明

### 5.1 按数量模式

1. **日期过滤**：
   - 优先使用 `data_date`（数据获取时间）
   - 如果没有 `data_date`，使用 `target_date`（预测时间）
   - 如果都没有指定，获取所有有数据的股票

2. **排序支持**：
   - `sort_type='turnover'`：按成交额排序（从数据库获取成交额数据）
   - `sort_type='random'`：随机排序
   - 默认：按股票代码排序

3. **数量限制**：
   - 如果指定了 `limit`，限制返回的股票数量

### 5.2 按股票代码模式

1. **从数据库获取股票名称**：
   - 根据 `data_date` 或 `target_date` 从数据库获取股票名称
   - 如果数据库查询失败，使用股票代码作为名称

2. **避免API调用**：
   - 不再调用 `data_source.get_stock_info()` API
   - 减少网络依赖

## 六、优势

### 6.1 避免API调用

- ✅ 不再调用 `ak.stock_zh_a_spot_em()` API
- ✅ 不再调用 `data_source.get_stock_info()` API
- ✅ 避免网络连接错误
- ✅ 提高执行成功率

### 6.2 性能提升

- ✅ 数据库查询比API调用更快
- ✅ 不需要等待网络响应
- ✅ 减少外部依赖

### 6.3 数据准确性

- ✅ 只分析数据库中有数据的股票
- ✅ 根据预测日期过滤，确保数据可用性
- ✅ 避免分析没有数据的股票

## 七、注意事项

1. **数据库依赖**：需要确保 `stock_history_data` 表中有数据
2. **日期过滤**：根据 `data_date` 或 `target_date` 过滤，确保只分析有数据的股票
3. **错误处理**：如果数据库查询失败，会返回错误信息，不会继续执行

## 八、测试建议

1. **功能测试**：
   - 点击"开始分析"按钮
   - 选择"按数量"模式
   - 验证是否从数据库获取股票列表
   - 验证是否不再调用API

2. **数据测试**：
   - 测试指定日期有数据的情况
   - 测试指定日期没有数据的情况
   - 测试没有指定日期的情况

3. **排序测试**：
   - 测试按成交额排序是否正常工作
   - 测试随机排序是否正常工作
   - 验证排序结果是否正确

4. **按股票代码测试**：
   - 选择"按股票代码"模式
   - 输入股票代码列表
   - 验证是否从数据库获取股票名称
   - 验证是否不再调用API

---

**修复完成时间**：2026-01-26
**修改文件**：`web_app.py`
**影响范围**：设置页面 -> 股票分析 -> "开始分析"按钮
