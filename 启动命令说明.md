# 启动命令说明

**创建时间**：2026-01-23  
**更新说明**：记录两个项目中所有需要启动的命令，标注必须/可选状态

---

## 📋 目录

- [smart_stock_advisor 项目](#smart_stock_advisor-项目)
- [news-analysis-system-main 项目](#news-analysis-system-main-项目)
- [启动顺序建议](#启动顺序建议)
- [更新记录](#更新记录)

---

## smart_stock_advisor 项目

### 🔴 必须启动的服务

#### 1. Web应用服务（必须）⭐

**功能**：提供Web界面，包括股票预测、设置管理、数据查看等功能

**启动方式**：

**方式1：使用批处理文件（推荐）**
```bash
cd d:\wjw_work\smart_stock_advisor
start_web.bat
# 或
run_web_app.bat
```

**方式2：直接运行Python**
```bash
cd d:\wjw_work\smart_stock_advisor
py web_app.py
# 或
python web_app.py
```

**访问地址**：http://localhost:5000

**说明**：
- ✅ **必须启动**：这是系统的主要入口
- ✅ 提供Web界面和API接口
- ✅ 默认端口：5000
- ✅ 支持用户登录、股票预测、设置管理等功能

**文件位置**：
- `web_app.py` - Web应用主文件
- `start_web.bat` - 启动脚本
- `run_web_app.bat` - 启动脚本（备用）

---

### 🟡 可选启动的服务

#### 2. 命令行预测工具（可选）

**功能**：命令行模式的股票预测工具

**启动方式**：
```bash
cd d:\wjw_work\smart_stock_advisor
py main.py
# 或使用快速开始脚本
快速开始.bat
```

**说明**：
- ⚠️ **可选**：如果使用Web界面，不需要此工具
- ✅ 适合命令行操作和脚本调用
- ✅ 支持预测模式、实时模式、监控模式

**文件位置**：
- `main.py` - 命令行工具主文件
- `快速开始.bat` - 快速启动脚本

---

#### 3. API服务器（可选）

**功能**：独立的API服务器，提供股票数据接口

**启动方式**：
```bash
cd d:\wjw_work\smart_stock_advisor
py api_server.py
```

**说明**：
- ⚠️ **可选**：Web应用已包含API功能
- ✅ 适合需要独立API服务的场景
- ✅ 轻量级HTTP服务器

**文件位置**：
- `api_server.py` - API服务器文件

---

### 🟢 测试/工具脚本（可选）

#### 4. 数据收集脚本（可选）

**功能**：批量收集股票历史数据

**启动方式**：
```bash
cd d:\wjw_work\smart_stock_advisor
py scripts\batch_collect_10years_data.py
# 或使用批处理
测试数据收集.bat
```

**说明**：
- ⚠️ **可选**：按需使用
- ✅ 用于初始化数据或补充历史数据
- ✅ 可以通过Web界面设置页面的"股票数据获取"功能替代

**文件位置**：
- `scripts\batch_collect_10years_data.py`
- `scripts\start_data_collection.bat`
- `测试数据收集.bat`

---

#### 5. 其他工具脚本（可选）

**功能**：各种数据检查、更新、测试工具

**常用脚本**：
```bash
# 更新技术指标
py scripts\batch_update_technical_indicators.py

# 计算单个股票指标
py scripts\calculate_indicators_for_stock.py

# 更新股票历史数据
py scripts\update_stock_history_daily.py

# 训练ML模型
py scripts\train_base_model.py
py scripts\train_ml_models.py

# 检查数据完整性
py scripts\check_single_stock.py
py scripts\check_ml_model_status.py
```

**说明**：
- ⚠️ **可选**：按需使用
- ✅ 大部分功能已集成到Web界面
- ✅ 适合批量操作和自动化脚本

---

## news-analysis-system-main 项目

### 🟡 推荐启动的服务

#### 1. 定时任务调度器（推荐）⭐

**功能**：统一管理新闻获取和LLM分析任务
- 新闻获取：每10分钟自动执行
- LLM分析：每2小时自动执行

**启动方式**：

**方式1：使用批处理文件（推荐）**
```bash
cd d:\wjw_work\news-analysis-system-main
start_scheduler.bat
```

**方式2：直接运行Python**
```bash
cd d:\wjw_work\news-analysis-system-main
py scheduler.py
```

**说明**：
- ⚠️ **推荐启动**：自动化定时执行新闻获取和LLM分析
- ✅ 无需人工干预
- ✅ 持续运行，不会遗漏
- ⚠️ 也可以通过Web界面手动启动（见下方）

**文件位置**：
- `scheduler.py` - 调度器主文件
- `start_scheduler.bat` - 启动脚本

**替代方案**：
- 如果不想启动scheduler.py，可以通过smart_stock_advisor的Web界面：
  - 新闻获取：使用"新闻抓取任务"功能
  - LLM分析：使用"启动LLM分析"按钮

---

### 🟢 测试/工具脚本（可选）

#### 2. 测试新闻获取（可选）

**功能**：测试新闻获取和股票匹配功能（单次运行）

**启动方式**：
```bash
cd d:\wjw_work\news-analysis-system-main
py test_get_news_once.py
```

**说明**：
- ⚠️ **可选**：用于测试和调试
- ✅ 单次运行，不会持续执行
- ✅ 适合测试新闻获取功能

**文件位置**：
- `test_get_news_once.py`

---

#### 3. 测试LLM分析（可选）

**功能**：测试LLM分析功能（单次运行）

**启动方式**：
```bash
cd d:\wjw_work\news-analysis-system-main
py test_llm_analysis.py
```

**说明**：
- ⚠️ **可选**：用于测试和调试
- ✅ 单次运行，不会持续执行
- ✅ 适合测试LLM分析功能
- ✅ 也可以通过Web界面启动（设置页面 → 新闻抓取 → 启动LLM分析）

**文件位置**：
- `test_llm_analysis.py`

---

#### 4. Streamlit Web界面（可选）

**功能**：基于Streamlit的新闻分析可视化界面

**启动方式**：
```bash
cd d:\wjw_work\news-analysis-system-main
streamlit run src/visualization/streamlit_web.py
```

**访问地址**：http://localhost:8501

**说明**：
- ⚠️ **可选**：用于查看新闻分析结果的可视化
- ✅ 显示高频关键词、市场情绪、分类总结等
- ✅ 适合查看LLM分析结果
- ⚠️ 需要安装streamlit：`pip install streamlit`

**文件位置**：
- `src/visualization/streamlit_web.py`

---

#### 5. 数据完整性检查（可选）

**功能**：检查新闻数据完整性

**启动方式**：
```bash
cd d:\wjw_work\news-analysis-system-main
py check_data_completeness.py
```

**说明**：
- ⚠️ **可选**：用于检查数据质量
- ✅ 显示数据统计信息
- ✅ 检查字段完整性

**文件位置**：
- `check_data_completeness.py`

---

## 🚀 启动顺序建议

### 最小化启动（仅核心功能）

**必须启动**：
1. ✅ `smart_stock_advisor\web_app.py` - Web应用服务

**说明**：
- 只启动Web应用即可使用基本功能
- 新闻获取和LLM分析可以通过Web界面手动操作

---

### 推荐启动（自动化运行）

**必须启动**：
1. ✅ `smart_stock_advisor\web_app.py` - Web应用服务

**推荐启动**：
2. ⭐ `news-analysis-system-main\scheduler.py` - 定时任务调度器

**说明**：
- Web应用提供界面和核心功能
- scheduler.py自动执行新闻获取和LLM分析
- 最佳实践，适合生产环境

---

### 完整启动（所有功能）

**必须启动**：
1. ✅ `smart_stock_advisor\web_app.py` - Web应用服务

**推荐启动**：
2. ⭐ `news-analysis-system-main\scheduler.py` - 定时任务调度器

**可选启动**（按需）：
3. ⚠️ `smart_stock_advisor\main.py` - 命令行预测工具（如果需要）
4. ⚠️ `smart_stock_advisor\api_server.py` - 独立API服务器（如果需要）

---

## 📊 启动命令汇总表

| 项目 | 服务/脚本 | 启动命令 | 必须/可选 | 说明 |
|------|----------|---------|----------|------|
| **smart_stock_advisor** | Web应用 | `py web_app.py` | 🔴 **必须** | Web界面和API |
| **smart_stock_advisor** | 命令行工具 | `py main.py` | 🟡 可选 | 命令行预测 |
| **smart_stock_advisor** | API服务器 | `py api_server.py` | 🟡 可选 | 独立API服务 |
| **news-analysis-system-main** | 定时调度器 | `py scheduler.py` | 🟡 **推荐** | 自动新闻获取和LLM分析 |
| **news-analysis-system-main** | 测试新闻获取 | `py test_get_news_once.py` | 🟢 可选 | 测试用 |
| **news-analysis-system-main** | 测试LLM分析 | `py test_llm_analysis.py` | 🟢 可选 | 测试用 |
| **news-analysis-system-main** | Streamlit界面 | `streamlit run src/visualization/streamlit_web.py` | 🟢 可选 | 可视化界面 |
| **news-analysis-system-main** | 数据检查 | `py check_data_completeness.py` | 🟢 可选 | 数据质量检查 |

---

## 🔧 快速启动脚本

### Windows批处理文件

**smart_stock_advisor**：
- `start_web.bat` - 启动Web应用
- `run_web_app.bat` - 启动Web应用（备用）
- `快速开始.bat` - 启动命令行工具

**news-analysis-system-main**：
- `start_scheduler.bat` - 启动定时调度器

---

## 💡 使用建议

### 开发/测试环境

**推荐启动**：
1. ✅ `smart_stock_advisor\web_app.py` - Web应用
2. ⚠️ 按需使用测试脚本

**说明**：
- 只启动Web应用即可
- 需要时手动操作或使用测试脚本

---

### 生产环境（推荐）

**推荐启动**：
1. ✅ `smart_stock_advisor\web_app.py` - Web应用
2. ⭐ `news-analysis-system-main\scheduler.py` - 定时调度器

**说明**：
- Web应用提供界面和核心功能
- scheduler.py自动执行新闻获取和LLM分析
- 设置开机自启（Windows任务计划程序）

---

### 轻量级使用

**推荐启动**：
1. ✅ `smart_stock_advisor\web_app.py` - Web应用

**说明**：
- 只启动Web应用
- 通过Web界面手动操作新闻获取和LLM分析
- 不需要额外的服务

---

## 📝 更新记录

### 2026-01-23

**新增**：
- ✅ 创建启动命令说明文档
- ✅ 记录smart_stock_advisor项目的启动命令
- ✅ 记录news-analysis-system-main项目的启动命令
- ✅ 标注必须/可选状态
- ✅ 添加启动顺序建议

**当前记录的命令**：
1. `smart_stock_advisor\web_app.py` - Web应用（必须）
2. `smart_stock_advisor\main.py` - 命令行工具（可选）
3. `smart_stock_advisor\api_server.py` - API服务器（可选）
4. `news-analysis-system-main\scheduler.py` - 定时调度器（推荐）
5. `news-analysis-system-main\test_get_news_once.py` - 测试新闻获取（可选）
6. `news-analysis-system-main\test_llm_analysis.py` - 测试LLM分析（可选）
7. `news-analysis-system-main\src\visualization\streamlit_web.py` - Streamlit界面（可选）
8. `news-analysis-system-main\check_data_completeness.py` - 数据检查（可选）

---

## 📰 新闻定时获取功能说明

### ✅ 功能确认

**设置页面的"新闻抓取任务"功能**：
- ✅ **是定时获取新闻**：支持间隔执行、每小时执行、每日执行
- ✅ **自动去重**：使用精确匹配（标题+来源）+ SimHash相似度检测
- ✅ **推荐间隔时间**：
  - smart_stock_advisor：30-60分钟（多源新闻）
  - news-analysis-system-main：10分钟（财联社新闻）

### 📊 推荐配置

**smart_stock_advisor（设置页面）**：
```
任务类型：全部（all）
调度方式：间隔执行（interval）
间隔时间：30-60分钟 ⭐
```

**news-analysis-system-main（scheduler.py）**：
```
新闻获取：每10分钟 ⭐
LLM分析：每2小时 ⭐
```

### 🔍 去重功能

**smart_stock_advisor**：
- ✅ 精确匹配（标题+来源）
- ✅ SimHash相似度检测
- ✅ 双重保障，去重效果更好

**news-analysis-system-main**：
- ✅ content_hash 检查
- ✅ 数据库唯一索引（uk_title_source）
- ✅ 性能高，去重效果良好

**详细说明**：参见 `smart_stock_advisor\docs\新闻定时获取功能说明.md`

---

## 🔄 后续更新

**当添加新的启动命令时，请在此文档中更新**：

1. 在相应的项目章节中添加新命令
2. 标注必须/可选状态
3. 添加说明和使用场景
4. 在"更新记录"中记录更新日期和内容

---

## ✅ 总结

**必须启动**：
- ✅ `smart_stock_advisor\web_app.py` - Web应用服务

**推荐启动**：
- ⭐ `news-analysis-system-main\scheduler.py` - 定时任务调度器（自动化）

**可选启动**：
- ⚠️ 其他所有脚本和服务（按需使用）

**最佳实践**：
- 🎯 生产环境：Web应用 + scheduler.py
- 🎯 开发环境：只启动Web应用，按需使用测试脚本
